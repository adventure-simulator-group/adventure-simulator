// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DevBootstrapGalleryArgs {
    pub bootstrap_token: String,
    pub offset: u32,
    pub count: u32,
}

impl From<DevBootstrapGalleryArgs> for super::Reducer {
    fn from(args: DevBootstrapGalleryArgs) -> Self {
        Self::DevBootstrapGallery {
            bootstrap_token: args.bootstrap_token,
            offset: args.offset,
            count: args.count,
        }
    }
}

impl __sdk::InModule for DevBootstrapGalleryArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `dev_bootstrap_gallery`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait dev_bootstrap_gallery {
    /// Request that the remote module invoke the reducer `dev_bootstrap_gallery` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`dev_bootstrap_gallery:dev_bootstrap_gallery_then`] to run a callback after the reducer completes.
    fn dev_bootstrap_gallery(
        &self,
        bootstrap_token: String,
        offset: u32,
        count: u32,
    ) -> __sdk::Result<()> {
        self.dev_bootstrap_gallery_then(bootstrap_token, offset, count, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `dev_bootstrap_gallery` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn dev_bootstrap_gallery_then(
        &self,
        bootstrap_token: String,
        offset: u32,
        count: u32,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl dev_bootstrap_gallery for super::RemoteReducers {
    fn dev_bootstrap_gallery_then(
        &self,
        bootstrap_token: String,
        offset: u32,
        count: u32,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DevBootstrapGalleryArgs {
                bootstrap_token,
                offset,
                count,
            },
            callback,
        )
    }
}
