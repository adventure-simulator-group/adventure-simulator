// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DefineShieldArgs {
    pub item_id: String,
    pub weight: f32,
    pub block: f32,
}

impl From<DefineShieldArgs> for super::Reducer {
    fn from(args: DefineShieldArgs) -> Self {
        Self::DefineShield {
            item_id: args.item_id,
            weight: args.weight,
            block: args.block,
        }
    }
}

impl __sdk::InModule for DefineShieldArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `define_shield`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait define_shield {
    /// Request that the remote module invoke the reducer `define_shield` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`define_shield:define_shield_then`] to run a callback after the reducer completes.
    fn define_shield(&self, item_id: String, weight: f32, block: f32) -> __sdk::Result<()> {
        self.define_shield_then(item_id, weight, block, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `define_shield` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn define_shield_then(
        &self,
        item_id: String,
        weight: f32,
        block: f32,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl define_shield for super::RemoteReducers {
    fn define_shield_then(
        &self,
        item_id: String,
        weight: f32,
        block: f32,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DefineShieldArgs {
                item_id,
                weight,
                block,
            },
            callback,
        )
    }
}
