// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct BuryCorpseArgs {
    pub actor_id: u64,
    pub corpse_id: String,
    pub action_id: String,
    pub expected_revision: u32,
}

impl From<BuryCorpseArgs> for super::Reducer {
    fn from(args: BuryCorpseArgs) -> Self {
        Self::BuryCorpse {
            actor_id: args.actor_id,
            corpse_id: args.corpse_id,
            action_id: args.action_id,
            expected_revision: args.expected_revision,
        }
    }
}

impl __sdk::InModule for BuryCorpseArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `bury_corpse`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait bury_corpse {
    /// Request that the remote module invoke the reducer `bury_corpse` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`bury_corpse:bury_corpse_then`] to run a callback after the reducer completes.
    fn bury_corpse(
        &self,
        actor_id: u64,
        corpse_id: String,
        action_id: String,
        expected_revision: u32,
    ) -> __sdk::Result<()> {
        self.bury_corpse_then(actor_id, corpse_id, action_id, expected_revision, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `bury_corpse` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn bury_corpse_then(
        &self,
        actor_id: u64,
        corpse_id: String,
        action_id: String,
        expected_revision: u32,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl bury_corpse for super::RemoteReducers {
    fn bury_corpse_then(
        &self,
        actor_id: u64,
        corpse_id: String,
        action_id: String,
        expected_revision: u32,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            BuryCorpseArgs {
                actor_id,
                corpse_id,
                action_id,
                expected_revision,
            },
            callback,
        )
    }
}
