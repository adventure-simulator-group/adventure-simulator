// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PutInventoryItemInContainerArgs {
    pub character_id: u64,
    pub child_scope: String,
    pub child_row_id: u64,
    pub parent_scope: String,
    pub parent_row_id: u64,
}

impl From<PutInventoryItemInContainerArgs> for super::Reducer {
    fn from(args: PutInventoryItemInContainerArgs) -> Self {
        Self::PutInventoryItemInContainer {
            character_id: args.character_id,
            child_scope: args.child_scope,
            child_row_id: args.child_row_id,
            parent_scope: args.parent_scope,
            parent_row_id: args.parent_row_id,
        }
    }
}

impl __sdk::InModule for PutInventoryItemInContainerArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `put_inventory_item_in_container`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait put_inventory_item_in_container {
    /// Request that the remote module invoke the reducer `put_inventory_item_in_container` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`put_inventory_item_in_container:put_inventory_item_in_container_then`] to run a callback after the reducer completes.
    fn put_inventory_item_in_container(
        &self,
        character_id: u64,
        child_scope: String,
        child_row_id: u64,
        parent_scope: String,
        parent_row_id: u64,
    ) -> __sdk::Result<()> {
        self.put_inventory_item_in_container_then(
            character_id,
            child_scope,
            child_row_id,
            parent_scope,
            parent_row_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `put_inventory_item_in_container` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn put_inventory_item_in_container_then(
        &self,
        character_id: u64,
        child_scope: String,
        child_row_id: u64,
        parent_scope: String,
        parent_row_id: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl put_inventory_item_in_container for super::RemoteReducers {
    fn put_inventory_item_in_container_then(
        &self,
        character_id: u64,
        child_scope: String,
        child_row_id: u64,
        parent_scope: String,
        parent_row_id: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            PutInventoryItemInContainerArgs {
                character_id,
                child_scope,
                child_row_id,
                parent_scope,
                parent_row_id,
            },
            callback,
        )
    }
}
