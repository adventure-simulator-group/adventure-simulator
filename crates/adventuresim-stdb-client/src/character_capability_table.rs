// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::character_capability_type::CharacterCapability;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `character_capability`.
///
/// Obtain a handle from the [`CharacterCapabilityTableAccess::character_capability`] method on [`super::RemoteTables`],
/// like `ctx.db.character_capability()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.character_capability().on_insert(...)`.
pub struct CharacterCapabilityTableHandle<'ctx> {
    imp: __sdk::TableHandle<CharacterCapability>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `character_capability`.
///
/// Implemented for [`super::RemoteTables`].
pub trait CharacterCapabilityTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`CharacterCapabilityTableHandle`], which mediates access to the table `character_capability`.
    fn character_capability(&self) -> CharacterCapabilityTableHandle<'_>;
}

impl CharacterCapabilityTableAccess for super::RemoteTables {
    fn character_capability(&self) -> CharacterCapabilityTableHandle<'_> {
        CharacterCapabilityTableHandle {
            imp: self
                .imp
                .get_table::<CharacterCapability>("character_capability"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct CharacterCapabilityInsertCallbackId(__sdk::CallbackId);
pub struct CharacterCapabilityDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for CharacterCapabilityTableHandle<'ctx> {
    type Row = CharacterCapability;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = CharacterCapability> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = CharacterCapabilityInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> CharacterCapabilityInsertCallbackId {
        CharacterCapabilityInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: CharacterCapabilityInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = CharacterCapabilityDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> CharacterCapabilityDeleteCallbackId {
        CharacterCapabilityDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: CharacterCapabilityDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<CharacterCapability>("character_capability");
    _table.add_unique_constraint::<u64>("character_id", |row| &row.character_id);
}
pub struct CharacterCapabilityUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for CharacterCapabilityTableHandle<'ctx> {
    type UpdateCallbackId = CharacterCapabilityUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> CharacterCapabilityUpdateCallbackId {
        CharacterCapabilityUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: CharacterCapabilityUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<CharacterCapability>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<CharacterCapability>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `character_id` unique index on the table `character_capability`,
/// which allows point queries on the field of the same name
/// via the [`CharacterCapabilityCharacterIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.character_capability().character_id().find(...)`.
pub struct CharacterCapabilityCharacterIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<CharacterCapability, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> CharacterCapabilityTableHandle<'ctx> {
    /// Get a handle on the `character_id` unique index on the table `character_capability`.
    pub fn character_id(&self) -> CharacterCapabilityCharacterIdUnique<'ctx> {
        CharacterCapabilityCharacterIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("character_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> CharacterCapabilityCharacterIdUnique<'ctx> {
    /// Find the subscribed row whose `character_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<CharacterCapability> {
        self.imp.find(col_val)
    }
}
