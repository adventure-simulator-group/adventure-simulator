// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CraftMedicationArgs {
    pub character_id: u64,
    pub disease_id: String,
    pub party_scope: bool,
}

impl From<CraftMedicationArgs> for super::Reducer {
    fn from(args: CraftMedicationArgs) -> Self {
        Self::CraftMedication {
            character_id: args.character_id,
            disease_id: args.disease_id,
            party_scope: args.party_scope,
        }
    }
}

impl __sdk::InModule for CraftMedicationArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `craft_medication`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait craft_medication {
    /// Request that the remote module invoke the reducer `craft_medication` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`craft_medication:craft_medication_then`] to run a callback after the reducer completes.
    fn craft_medication(
        &self,
        character_id: u64,
        disease_id: String,
        party_scope: bool,
    ) -> __sdk::Result<()> {
        self.craft_medication_then(character_id, disease_id, party_scope, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `craft_medication` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn craft_medication_then(
        &self,
        character_id: u64,
        disease_id: String,
        party_scope: bool,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl craft_medication for super::RemoteReducers {
    fn craft_medication_then(
        &self,
        character_id: u64,
        disease_id: String,
        party_scope: bool,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CraftMedicationArgs {
                character_id,
                disease_id,
                party_scope,
            },
            callback,
        )
    }
}
