// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct LoadOutbreakDemoArgs {
    pub character_id: u64,
}

impl From<LoadOutbreakDemoArgs> for super::Reducer {
    fn from(args: LoadOutbreakDemoArgs) -> Self {
        Self::LoadOutbreakDemo {
            character_id: args.character_id,
        }
    }
}

impl __sdk::InModule for LoadOutbreakDemoArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `load_outbreak_demo`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait load_outbreak_demo {
    /// Request that the remote module invoke the reducer `load_outbreak_demo` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`load_outbreak_demo:load_outbreak_demo_then`] to run a callback after the reducer completes.
    fn load_outbreak_demo(&self, character_id: u64) -> __sdk::Result<()> {
        self.load_outbreak_demo_then(character_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `load_outbreak_demo` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn load_outbreak_demo_then(
        &self,
        character_id: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl load_outbreak_demo for super::RemoteReducers {
    fn load_outbreak_demo_then(
        &self,
        character_id: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(LoadOutbreakDemoArgs { character_id }, callback)
    }
}
