// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::character_needs_type::CharacterNeeds;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `character_needs`.
///
/// Obtain a handle from the [`CharacterNeedsTableAccess::character_needs`] method on [`super::RemoteTables`],
/// like `ctx.db.character_needs()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.character_needs().on_insert(...)`.
pub struct CharacterNeedsTableHandle<'ctx> {
    imp: __sdk::TableHandle<CharacterNeeds>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `character_needs`.
///
/// Implemented for [`super::RemoteTables`].
pub trait CharacterNeedsTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`CharacterNeedsTableHandle`], which mediates access to the table `character_needs`.
    fn character_needs(&self) -> CharacterNeedsTableHandle<'_>;
}

impl CharacterNeedsTableAccess for super::RemoteTables {
    fn character_needs(&self) -> CharacterNeedsTableHandle<'_> {
        CharacterNeedsTableHandle {
            imp: self.imp.get_table::<CharacterNeeds>("character_needs"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct CharacterNeedsInsertCallbackId(__sdk::CallbackId);
pub struct CharacterNeedsDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for CharacterNeedsTableHandle<'ctx> {
    type Row = CharacterNeeds;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = CharacterNeeds> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = CharacterNeedsInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> CharacterNeedsInsertCallbackId {
        CharacterNeedsInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: CharacterNeedsInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = CharacterNeedsDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> CharacterNeedsDeleteCallbackId {
        CharacterNeedsDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: CharacterNeedsDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<CharacterNeeds>("character_needs");
    _table.add_unique_constraint::<u64>("character_id", |row| &row.character_id);
}
pub struct CharacterNeedsUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for CharacterNeedsTableHandle<'ctx> {
    type UpdateCallbackId = CharacterNeedsUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> CharacterNeedsUpdateCallbackId {
        CharacterNeedsUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: CharacterNeedsUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<CharacterNeeds>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<CharacterNeeds>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `character_id` unique index on the table `character_needs`,
/// which allows point queries on the field of the same name
/// via the [`CharacterNeedsCharacterIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.character_needs().character_id().find(...)`.
pub struct CharacterNeedsCharacterIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<CharacterNeeds, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> CharacterNeedsTableHandle<'ctx> {
    /// Get a handle on the `character_id` unique index on the table `character_needs`.
    pub fn character_id(&self) -> CharacterNeedsCharacterIdUnique<'ctx> {
        CharacterNeedsCharacterIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("character_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> CharacterNeedsCharacterIdUnique<'ctx> {
    /// Find the subscribed row whose `character_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<CharacterNeeds> {
        self.imp.find(col_val)
    }
}
