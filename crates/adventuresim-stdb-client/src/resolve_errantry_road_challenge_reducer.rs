// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ResolveErrantryRoadChallengeArgs {
    pub character_id: u64,
    pub challenge_id: String,
    pub expected_revision: u32,
    pub choice: String,
    pub action_id: String,
}

impl From<ResolveErrantryRoadChallengeArgs> for super::Reducer {
    fn from(args: ResolveErrantryRoadChallengeArgs) -> Self {
        Self::ResolveErrantryRoadChallenge {
            character_id: args.character_id,
            challenge_id: args.challenge_id,
            expected_revision: args.expected_revision,
            choice: args.choice,
            action_id: args.action_id,
        }
    }
}

impl __sdk::InModule for ResolveErrantryRoadChallengeArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `resolve_errantry_road_challenge`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait resolve_errantry_road_challenge {
    /// Request that the remote module invoke the reducer `resolve_errantry_road_challenge` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`resolve_errantry_road_challenge:resolve_errantry_road_challenge_then`] to run a callback after the reducer completes.
    fn resolve_errantry_road_challenge(
        &self,
        character_id: u64,
        challenge_id: String,
        expected_revision: u32,
        choice: String,
        action_id: String,
    ) -> __sdk::Result<()> {
        self.resolve_errantry_road_challenge_then(
            character_id,
            challenge_id,
            expected_revision,
            choice,
            action_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `resolve_errantry_road_challenge` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn resolve_errantry_road_challenge_then(
        &self,
        character_id: u64,
        challenge_id: String,
        expected_revision: u32,
        choice: String,
        action_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl resolve_errantry_road_challenge for super::RemoteReducers {
    fn resolve_errantry_road_challenge_then(
        &self,
        character_id: u64,
        challenge_id: String,
        expected_revision: u32,
        choice: String,
        action_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ResolveErrantryRoadChallengeArgs {
                character_id,
                challenge_id,
                expected_revision,
                choice,
                action_id,
            },
            callback,
        )
    }
}
