// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SpawnDeveloperQuestArgs {
    pub character_id: u64,
    pub definition_json: String,
    pub allow_implausible: bool,
}

impl From<SpawnDeveloperQuestArgs> for super::Reducer {
    fn from(args: SpawnDeveloperQuestArgs) -> Self {
        Self::SpawnDeveloperQuest {
            character_id: args.character_id,
            definition_json: args.definition_json,
            allow_implausible: args.allow_implausible,
        }
    }
}

impl __sdk::InModule for SpawnDeveloperQuestArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `spawn_developer_quest`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait spawn_developer_quest {
    /// Request that the remote module invoke the reducer `spawn_developer_quest` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`spawn_developer_quest:spawn_developer_quest_then`] to run a callback after the reducer completes.
    fn spawn_developer_quest(
        &self,
        character_id: u64,
        definition_json: String,
        allow_implausible: bool,
    ) -> __sdk::Result<()> {
        self.spawn_developer_quest_then(character_id, definition_json, allow_implausible, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `spawn_developer_quest` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn spawn_developer_quest_then(
        &self,
        character_id: u64,
        definition_json: String,
        allow_implausible: bool,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl spawn_developer_quest for super::RemoteReducers {
    fn spawn_developer_quest_then(
        &self,
        character_id: u64,
        definition_json: String,
        allow_implausible: bool,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SpawnDeveloperQuestArgs {
                character_id,
                definition_json,
                allow_implausible,
            },
            callback,
        )
    }
}
