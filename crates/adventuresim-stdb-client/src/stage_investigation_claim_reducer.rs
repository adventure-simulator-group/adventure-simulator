// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct StageInvestigationClaimArgs {
    pub character_id: u64,
    pub receipt_id: String,
    pub pipeline_json: String,
    pub public_case_id: String,
    pub safe_source_label: String,
    pub conflict_group: String,
    pub correction_of_belief_id: String,
}

impl From<StageInvestigationClaimArgs> for super::Reducer {
    fn from(args: StageInvestigationClaimArgs) -> Self {
        Self::StageInvestigationClaim {
            character_id: args.character_id,
            receipt_id: args.receipt_id,
            pipeline_json: args.pipeline_json,
            public_case_id: args.public_case_id,
            safe_source_label: args.safe_source_label,
            conflict_group: args.conflict_group,
            correction_of_belief_id: args.correction_of_belief_id,
        }
    }
}

impl __sdk::InModule for StageInvestigationClaimArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `stage_investigation_claim`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait stage_investigation_claim {
    /// Request that the remote module invoke the reducer `stage_investigation_claim` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`stage_investigation_claim:stage_investigation_claim_then`] to run a callback after the reducer completes.
    fn stage_investigation_claim(
        &self,
        character_id: u64,
        receipt_id: String,
        pipeline_json: String,
        public_case_id: String,
        safe_source_label: String,
        conflict_group: String,
        correction_of_belief_id: String,
    ) -> __sdk::Result<()> {
        self.stage_investigation_claim_then(
            character_id,
            receipt_id,
            pipeline_json,
            public_case_id,
            safe_source_label,
            conflict_group,
            correction_of_belief_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `stage_investigation_claim` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn stage_investigation_claim_then(
        &self,
        character_id: u64,
        receipt_id: String,
        pipeline_json: String,
        public_case_id: String,
        safe_source_label: String,
        conflict_group: String,
        correction_of_belief_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl stage_investigation_claim for super::RemoteReducers {
    fn stage_investigation_claim_then(
        &self,
        character_id: u64,
        receipt_id: String,
        pipeline_json: String,
        public_case_id: String,
        safe_source_label: String,
        conflict_group: String,
        correction_of_belief_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            StageInvestigationClaimArgs {
                character_id,
                receipt_id,
                pipeline_json,
                public_case_id,
                safe_source_label,
                conflict_group,
                correction_of_belief_id,
            },
            callback,
        )
    }
}
