// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{
	self as __sdk,
	__lib,
	__sats,
	__ws,
};


#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RenameSavedRecruitmentRoleArgs {
    pub owner_id: u64,
    pub role_id: u64,
    pub name: String,
}

impl From<RenameSavedRecruitmentRoleArgs> for super::Reducer {
    fn from(args: RenameSavedRecruitmentRoleArgs) -> Self {
        Self::RenameSavedRecruitmentRole {
            owner_id: args.owner_id,
            role_id: args.role_id,
            name: args.name,
}
}
}

impl __sdk::InModule for RenameSavedRecruitmentRoleArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `rename_saved_recruitment_role`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait rename_saved_recruitment_role {
    /// Request that the remote module invoke the reducer `rename_saved_recruitment_role` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`rename_saved_recruitment_role:rename_saved_recruitment_role_then`] to run a callback after the reducer completes.
    fn rename_saved_recruitment_role(&self, owner_id: u64,
role_id: u64,
name: String,
) -> __sdk::Result<()> {
        self.rename_saved_recruitment_role_then(owner_id, role_id, name,  |_, _| {})
    }

    /// Request that the remote module invoke the reducer `rename_saved_recruitment_role` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn rename_saved_recruitment_role_then(
        &self,
        owner_id: u64,
role_id: u64,
name: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl rename_saved_recruitment_role for super::RemoteReducers {
    fn rename_saved_recruitment_role_then(
        &self,
        owner_id: u64,
role_id: u64,
name: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(RenameSavedRecruitmentRoleArgs { owner_id, role_id, name,  }, callback)
    }
}

