// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CreateTacticalServerArgs {
    pub mission_id: String,
    pub scene_key: String,
    pub addr: String,
    pub cert_digest: String,
}

impl From<CreateTacticalServerArgs> for super::Reducer {
    fn from(args: CreateTacticalServerArgs) -> Self {
        Self::CreateTacticalServer {
            mission_id: args.mission_id,
            scene_key: args.scene_key,
            addr: args.addr,
            cert_digest: args.cert_digest,
        }
    }
}

impl __sdk::InModule for CreateTacticalServerArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `create_tactical_server`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait create_tactical_server {
    /// Request that the remote module invoke the reducer `create_tactical_server` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`create_tactical_server:create_tactical_server_then`] to run a callback after the reducer completes.
    fn create_tactical_server(
        &self,
        mission_id: String,
        scene_key: String,
        addr: String,
        cert_digest: String,
    ) -> __sdk::Result<()> {
        self.create_tactical_server_then(mission_id, scene_key, addr, cert_digest, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `create_tactical_server` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn create_tactical_server_then(
        &self,
        mission_id: String,
        scene_key: String,
        addr: String,
        cert_digest: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl create_tactical_server for super::RemoteReducers {
    fn create_tactical_server_then(
        &self,
        mission_id: String,
        scene_key: String,
        addr: String,
        cert_digest: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CreateTacticalServerArgs {
                mission_id,
                scene_key,
                addr,
                cert_digest,
            },
            callback,
        )
    }
}
