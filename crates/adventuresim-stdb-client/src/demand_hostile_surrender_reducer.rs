// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DemandHostileSurrenderArgs {
    pub actor_id: u64,
    pub case_site_id: String,
    pub spokesman_id: u64,
    pub context_ref: String,
    pub expected_revision: u32,
    pub action_id: String,
}

impl From<DemandHostileSurrenderArgs> for super::Reducer {
    fn from(args: DemandHostileSurrenderArgs) -> Self {
        Self::DemandHostileSurrender {
            actor_id: args.actor_id,
            case_site_id: args.case_site_id,
            spokesman_id: args.spokesman_id,
            context_ref: args.context_ref,
            expected_revision: args.expected_revision,
            action_id: args.action_id,
        }
    }
}

impl __sdk::InModule for DemandHostileSurrenderArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `demand_hostile_surrender`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait demand_hostile_surrender {
    /// Request that the remote module invoke the reducer `demand_hostile_surrender` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`demand_hostile_surrender:demand_hostile_surrender_then`] to run a callback after the reducer completes.
    fn demand_hostile_surrender(
        &self,
        actor_id: u64,
        case_site_id: String,
        spokesman_id: u64,
        context_ref: String,
        expected_revision: u32,
        action_id: String,
    ) -> __sdk::Result<()> {
        self.demand_hostile_surrender_then(
            actor_id,
            case_site_id,
            spokesman_id,
            context_ref,
            expected_revision,
            action_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `demand_hostile_surrender` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn demand_hostile_surrender_then(
        &self,
        actor_id: u64,
        case_site_id: String,
        spokesman_id: u64,
        context_ref: String,
        expected_revision: u32,
        action_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl demand_hostile_surrender for super::RemoteReducers {
    fn demand_hostile_surrender_then(
        &self,
        actor_id: u64,
        case_site_id: String,
        spokesman_id: u64,
        context_ref: String,
        expected_revision: u32,
        action_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DemandHostileSurrenderArgs {
                actor_id,
                case_site_id,
                spokesman_id,
                context_ref,
                expected_revision,
                action_id,
            },
            callback,
        )
    }
}
