// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ExhumeCorpseArgs {
    pub actor_id: u64,
    pub corpse_id: String,
    pub action_id: String,
    pub expected_revision: u32,
    pub confirm_unauthorized: bool,
}

impl From<ExhumeCorpseArgs> for super::Reducer {
    fn from(args: ExhumeCorpseArgs) -> Self {
        Self::ExhumeCorpse {
            actor_id: args.actor_id,
            corpse_id: args.corpse_id,
            action_id: args.action_id,
            expected_revision: args.expected_revision,
            confirm_unauthorized: args.confirm_unauthorized,
        }
    }
}

impl __sdk::InModule for ExhumeCorpseArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `exhume_corpse`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait exhume_corpse {
    /// Request that the remote module invoke the reducer `exhume_corpse` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`exhume_corpse:exhume_corpse_then`] to run a callback after the reducer completes.
    fn exhume_corpse(
        &self,
        actor_id: u64,
        corpse_id: String,
        action_id: String,
        expected_revision: u32,
        confirm_unauthorized: bool,
    ) -> __sdk::Result<()> {
        self.exhume_corpse_then(
            actor_id,
            corpse_id,
            action_id,
            expected_revision,
            confirm_unauthorized,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `exhume_corpse` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn exhume_corpse_then(
        &self,
        actor_id: u64,
        corpse_id: String,
        action_id: String,
        expected_revision: u32,
        confirm_unauthorized: bool,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl exhume_corpse for super::RemoteReducers {
    fn exhume_corpse_then(
        &self,
        actor_id: u64,
        corpse_id: String,
        action_id: String,
        expected_revision: u32,
        confirm_unauthorized: bool,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ExhumeCorpseArgs {
                actor_id,
                corpse_id,
                action_id,
                expected_revision,
                confirm_unauthorized,
            },
            callback,
        )
    }
}
