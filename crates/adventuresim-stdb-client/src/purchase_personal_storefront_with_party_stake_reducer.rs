// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PurchasePersonalStorefrontWithPartyStakeArgs {
    pub character_id: u64,
    pub settlement_id: String,
    pub service_id: String,
    pub provider_resident_character_id: u64,
    pub item_id: String,
    pub quantity: u32,
    pub maximum_unit_price: u64,
    pub maximum_personal_payment: u64,
    pub maximum_stake_payment: u64,
}

impl From<PurchasePersonalStorefrontWithPartyStakeArgs> for super::Reducer {
    fn from(args: PurchasePersonalStorefrontWithPartyStakeArgs) -> Self {
        Self::PurchasePersonalStorefrontWithPartyStake {
            character_id: args.character_id,
            settlement_id: args.settlement_id,
            service_id: args.service_id,
            provider_resident_character_id: args.provider_resident_character_id,
            item_id: args.item_id,
            quantity: args.quantity,
            maximum_unit_price: args.maximum_unit_price,
            maximum_personal_payment: args.maximum_personal_payment,
            maximum_stake_payment: args.maximum_stake_payment,
        }
    }
}

impl __sdk::InModule for PurchasePersonalStorefrontWithPartyStakeArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `purchase_personal_storefront_with_party_stake`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait purchase_personal_storefront_with_party_stake {
    /// Request that the remote module invoke the reducer `purchase_personal_storefront_with_party_stake` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`purchase_personal_storefront_with_party_stake:purchase_personal_storefront_with_party_stake_then`] to run a callback after the reducer completes.
    fn purchase_personal_storefront_with_party_stake(
        &self,
        character_id: u64,
        settlement_id: String,
        service_id: String,
        provider_resident_character_id: u64,
        item_id: String,
        quantity: u32,
        maximum_unit_price: u64,
        maximum_personal_payment: u64,
        maximum_stake_payment: u64,
    ) -> __sdk::Result<()> {
        self.purchase_personal_storefront_with_party_stake_then(
            character_id,
            settlement_id,
            service_id,
            provider_resident_character_id,
            item_id,
            quantity,
            maximum_unit_price,
            maximum_personal_payment,
            maximum_stake_payment,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `purchase_personal_storefront_with_party_stake` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn purchase_personal_storefront_with_party_stake_then(
        &self,
        character_id: u64,
        settlement_id: String,
        service_id: String,
        provider_resident_character_id: u64,
        item_id: String,
        quantity: u32,
        maximum_unit_price: u64,
        maximum_personal_payment: u64,
        maximum_stake_payment: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl purchase_personal_storefront_with_party_stake for super::RemoteReducers {
    fn purchase_personal_storefront_with_party_stake_then(
        &self,
        character_id: u64,
        settlement_id: String,
        service_id: String,
        provider_resident_character_id: u64,
        item_id: String,
        quantity: u32,
        maximum_unit_price: u64,
        maximum_personal_payment: u64,
        maximum_stake_payment: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            PurchasePersonalStorefrontWithPartyStakeArgs {
                character_id,
                settlement_id,
                service_id,
                provider_resident_character_id,
                item_id,
                quantity,
                maximum_unit_price,
                maximum_personal_payment,
                maximum_stake_payment,
            },
            callback,
        )
    }
}
