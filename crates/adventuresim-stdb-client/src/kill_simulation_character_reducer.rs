// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct KillSimulationCharacterArgs {
    pub nonce: String,
    pub character_id: u64,
}

impl From<KillSimulationCharacterArgs> for super::Reducer {
    fn from(args: KillSimulationCharacterArgs) -> Self {
        Self::KillSimulationCharacter {
            nonce: args.nonce,
            character_id: args.character_id,
        }
    }
}

impl __sdk::InModule for KillSimulationCharacterArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `kill_simulation_character`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait kill_simulation_character {
    /// Request that the remote module invoke the reducer `kill_simulation_character` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`kill_simulation_character:kill_simulation_character_then`] to run a callback after the reducer completes.
    fn kill_simulation_character(&self, nonce: String, character_id: u64) -> __sdk::Result<()> {
        self.kill_simulation_character_then(nonce, character_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `kill_simulation_character` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn kill_simulation_character_then(
        &self,
        nonce: String,
        character_id: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl kill_simulation_character for super::RemoteReducers {
    fn kill_simulation_character_then(
        &self,
        nonce: String,
        character_id: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            KillSimulationCharacterArgs {
                nonce,
                character_id,
            },
            callback,
        )
    }
}
