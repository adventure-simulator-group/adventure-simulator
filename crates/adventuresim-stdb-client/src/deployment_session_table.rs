// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::deployment_session_type::DeploymentSession;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `deployment_session`.
///
/// Obtain a handle from the [`DeploymentSessionTableAccess::deployment_session`] method on [`super::RemoteTables`],
/// like `ctx.db.deployment_session()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.deployment_session().on_insert(...)`.
pub struct DeploymentSessionTableHandle<'ctx> {
    imp: __sdk::TableHandle<DeploymentSession>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `deployment_session`.
///
/// Implemented for [`super::RemoteTables`].
pub trait DeploymentSessionTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`DeploymentSessionTableHandle`], which mediates access to the table `deployment_session`.
    fn deployment_session(&self) -> DeploymentSessionTableHandle<'_>;
}

impl DeploymentSessionTableAccess for super::RemoteTables {
    fn deployment_session(&self) -> DeploymentSessionTableHandle<'_> {
        DeploymentSessionTableHandle {
            imp: self
                .imp
                .get_table::<DeploymentSession>("deployment_session"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct DeploymentSessionInsertCallbackId(__sdk::CallbackId);
pub struct DeploymentSessionDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for DeploymentSessionTableHandle<'ctx> {
    type Row = DeploymentSession;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = DeploymentSession> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = DeploymentSessionInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> DeploymentSessionInsertCallbackId {
        DeploymentSessionInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: DeploymentSessionInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = DeploymentSessionDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> DeploymentSessionDeleteCallbackId {
        DeploymentSessionDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: DeploymentSessionDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<DeploymentSession>("deployment_session");
    _table.add_unique_constraint::<String>("mission_id", |row| &row.mission_id);
}
pub struct DeploymentSessionUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for DeploymentSessionTableHandle<'ctx> {
    type UpdateCallbackId = DeploymentSessionUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> DeploymentSessionUpdateCallbackId {
        DeploymentSessionUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: DeploymentSessionUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<DeploymentSession>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<DeploymentSession>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `mission_id` unique index on the table `deployment_session`,
/// which allows point queries on the field of the same name
/// via the [`DeploymentSessionMissionIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.deployment_session().mission_id().find(...)`.
pub struct DeploymentSessionMissionIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<DeploymentSession, String>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> DeploymentSessionTableHandle<'ctx> {
    /// Get a handle on the `mission_id` unique index on the table `deployment_session`.
    pub fn mission_id(&self) -> DeploymentSessionMissionIdUnique<'ctx> {
        DeploymentSessionMissionIdUnique {
            imp: self.imp.get_unique_constraint::<String>("mission_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> DeploymentSessionMissionIdUnique<'ctx> {
    /// Find the subscribed row whose `mission_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &String) -> Option<DeploymentSession> {
        self.imp.find(col_val)
    }
}
