// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ScheduleWeddingArgs {
    pub first_character_id: u64,
    pub second_character_id: u64,
}

impl From<ScheduleWeddingArgs> for super::Reducer {
    fn from(args: ScheduleWeddingArgs) -> Self {
        Self::ScheduleWedding {
            first_character_id: args.first_character_id,
            second_character_id: args.second_character_id,
        }
    }
}

impl __sdk::InModule for ScheduleWeddingArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `schedule_wedding`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait schedule_wedding {
    /// Request that the remote module invoke the reducer `schedule_wedding` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`schedule_wedding:schedule_wedding_then`] to run a callback after the reducer completes.
    fn schedule_wedding(
        &self,
        first_character_id: u64,
        second_character_id: u64,
    ) -> __sdk::Result<()> {
        self.schedule_wedding_then(first_character_id, second_character_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `schedule_wedding` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn schedule_wedding_then(
        &self,
        first_character_id: u64,
        second_character_id: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl schedule_wedding for super::RemoteReducers {
    fn schedule_wedding_then(
        &self,
        first_character_id: u64,
        second_character_id: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ScheduleWeddingArgs {
                first_character_id,
                second_character_id,
            },
            callback,
        )
    }
}
