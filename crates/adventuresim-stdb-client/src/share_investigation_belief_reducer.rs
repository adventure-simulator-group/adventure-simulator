// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ShareInvestigationBeliefArgs {
    pub sender_id: u64,
    pub recipient_id: u64,
    pub source_belief_id: String,
    pub action_id: String,
}

impl From<ShareInvestigationBeliefArgs> for super::Reducer {
    fn from(args: ShareInvestigationBeliefArgs) -> Self {
        Self::ShareInvestigationBelief {
            sender_id: args.sender_id,
            recipient_id: args.recipient_id,
            source_belief_id: args.source_belief_id,
            action_id: args.action_id,
        }
    }
}

impl __sdk::InModule for ShareInvestigationBeliefArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `share_investigation_belief`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait share_investigation_belief {
    /// Request that the remote module invoke the reducer `share_investigation_belief` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`share_investigation_belief:share_investigation_belief_then`] to run a callback after the reducer completes.
    fn share_investigation_belief(
        &self,
        sender_id: u64,
        recipient_id: u64,
        source_belief_id: String,
        action_id: String,
    ) -> __sdk::Result<()> {
        self.share_investigation_belief_then(
            sender_id,
            recipient_id,
            source_belief_id,
            action_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `share_investigation_belief` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn share_investigation_belief_then(
        &self,
        sender_id: u64,
        recipient_id: u64,
        source_belief_id: String,
        action_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl share_investigation_belief for super::RemoteReducers {
    fn share_investigation_belief_then(
        &self,
        sender_id: u64,
        recipient_id: u64,
        source_belief_id: String,
        action_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ShareInvestigationBeliefArgs {
                sender_id,
                recipient_id,
                source_belief_id,
                action_id,
            },
            callback,
        )
    }
}
