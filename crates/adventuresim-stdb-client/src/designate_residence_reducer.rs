// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DesignateResidenceArgs {
    pub character_id: u64,
    pub holding_id: String,
}

impl From<DesignateResidenceArgs> for super::Reducer {
    fn from(args: DesignateResidenceArgs) -> Self {
        Self::DesignateResidence {
            character_id: args.character_id,
            holding_id: args.holding_id,
        }
    }
}

impl __sdk::InModule for DesignateResidenceArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `designate_residence`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait designate_residence {
    /// Request that the remote module invoke the reducer `designate_residence` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`designate_residence:designate_residence_then`] to run a callback after the reducer completes.
    fn designate_residence(&self, character_id: u64, holding_id: String) -> __sdk::Result<()> {
        self.designate_residence_then(character_id, holding_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `designate_residence` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn designate_residence_then(
        &self,
        character_id: u64,
        holding_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl designate_residence for super::RemoteReducers {
    fn designate_residence_then(
        &self,
        character_id: u64,
        holding_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DesignateResidenceArgs {
                character_id,
                holding_id,
            },
            callback,
        )
    }
}
