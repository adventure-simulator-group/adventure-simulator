// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::tactical_server_type::TacticalServer;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct EndTacticalServerByInstanceArgs {
    pub server: TacticalServer,
    pub success: bool,
    pub xp_gained: i32,
}

impl From<EndTacticalServerByInstanceArgs> for super::Reducer {
    fn from(args: EndTacticalServerByInstanceArgs) -> Self {
        Self::EndTacticalServerByInstance {
            server: args.server,
            success: args.success,
            xp_gained: args.xp_gained,
        }
    }
}

impl __sdk::InModule for EndTacticalServerByInstanceArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `end_tactical_server_by_instance`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait end_tactical_server_by_instance {
    /// Request that the remote module invoke the reducer `end_tactical_server_by_instance` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`end_tactical_server_by_instance:end_tactical_server_by_instance_then`] to run a callback after the reducer completes.
    fn end_tactical_server_by_instance(
        &self,
        server: TacticalServer,
        success: bool,
        xp_gained: i32,
    ) -> __sdk::Result<()> {
        self.end_tactical_server_by_instance_then(server, success, xp_gained, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `end_tactical_server_by_instance` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn end_tactical_server_by_instance_then(
        &self,
        server: TacticalServer,
        success: bool,
        xp_gained: i32,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl end_tactical_server_by_instance for super::RemoteReducers {
    fn end_tactical_server_by_instance_then(
        &self,
        server: TacticalServer,
        success: bool,
        xp_gained: i32,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            EndTacticalServerByInstanceArgs {
                server,
                success,
                xp_gained,
            },
            callback,
        )
    }
}
