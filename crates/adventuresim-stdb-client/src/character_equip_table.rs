// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::character_equip_type::CharacterEquip;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `character_equip`.
///
/// Obtain a handle from the [`CharacterEquipTableAccess::character_equip`] method on [`super::RemoteTables`],
/// like `ctx.db.character_equip()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.character_equip().on_insert(...)`.
pub struct CharacterEquipTableHandle<'ctx> {
    imp: __sdk::TableHandle<CharacterEquip>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `character_equip`.
///
/// Implemented for [`super::RemoteTables`].
pub trait CharacterEquipTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`CharacterEquipTableHandle`], which mediates access to the table `character_equip`.
    fn character_equip(&self) -> CharacterEquipTableHandle<'_>;
}

impl CharacterEquipTableAccess for super::RemoteTables {
    fn character_equip(&self) -> CharacterEquipTableHandle<'_> {
        CharacterEquipTableHandle {
            imp: self.imp.get_table::<CharacterEquip>("character_equip"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct CharacterEquipInsertCallbackId(__sdk::CallbackId);
pub struct CharacterEquipDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for CharacterEquipTableHandle<'ctx> {
    type Row = CharacterEquip;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = CharacterEquip> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = CharacterEquipInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> CharacterEquipInsertCallbackId {
        CharacterEquipInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: CharacterEquipInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = CharacterEquipDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> CharacterEquipDeleteCallbackId {
        CharacterEquipDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: CharacterEquipDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct CharacterEquipUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for CharacterEquipTableHandle<'ctx> {
    type UpdateCallbackId = CharacterEquipUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> CharacterEquipUpdateCallbackId {
        CharacterEquipUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: CharacterEquipUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `character_id` unique index on the table `character_equip`,
/// which allows point queries on the field of the same name
/// via the [`CharacterEquipCharacterIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.character_equip().character_id().find(...)`.
pub struct CharacterEquipCharacterIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<CharacterEquip, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> CharacterEquipTableHandle<'ctx> {
    /// Get a handle on the `character_id` unique index on the table `character_equip`.
    pub fn character_id(&self) -> CharacterEquipCharacterIdUnique<'ctx> {
        CharacterEquipCharacterIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("character_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> CharacterEquipCharacterIdUnique<'ctx> {
    /// Find the subscribed row whose `character_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<CharacterEquip> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<CharacterEquip>("character_equip");
    _table.add_unique_constraint::<u64>("character_id", |row| &row.character_id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<CharacterEquip>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<CharacterEquip>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `CharacterEquip`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait character_equipQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `CharacterEquip`.
    fn character_equip(&self) -> __sdk::__query_builder::Table<CharacterEquip>;
}

impl character_equipQueryTableAccess for __sdk::QueryTableAccessor {
    fn character_equip(&self) -> __sdk::__query_builder::Table<CharacterEquip> {
        __sdk::__query_builder::Table::new("character_equip")
    }
}
