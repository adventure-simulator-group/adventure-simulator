// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::battle_result_type::BattleResult;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `battle_result`.
///
/// Obtain a handle from the [`BattleResultTableAccess::battle_result`] method on [`super::RemoteTables`],
/// like `ctx.db.battle_result()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.battle_result().on_insert(...)`.
pub struct BattleResultTableHandle<'ctx> {
    imp: __sdk::TableHandle<BattleResult>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `battle_result`.
///
/// Implemented for [`super::RemoteTables`].
pub trait BattleResultTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`BattleResultTableHandle`], which mediates access to the table `battle_result`.
    fn battle_result(&self) -> BattleResultTableHandle<'_>;
}

impl BattleResultTableAccess for super::RemoteTables {
    fn battle_result(&self) -> BattleResultTableHandle<'_> {
        BattleResultTableHandle {
            imp: self.imp.get_table::<BattleResult>("battle_result"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct BattleResultInsertCallbackId(__sdk::CallbackId);
pub struct BattleResultDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for BattleResultTableHandle<'ctx> {
    type Row = BattleResult;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = BattleResult> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = BattleResultInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> BattleResultInsertCallbackId {
        BattleResultInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: BattleResultInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = BattleResultDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> BattleResultDeleteCallbackId {
        BattleResultDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: BattleResultDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<BattleResult>("battle_result");
    _table.add_unique_constraint::<String>("quest_id", |row| &row.quest_id);
}
pub struct BattleResultUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for BattleResultTableHandle<'ctx> {
    type UpdateCallbackId = BattleResultUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> BattleResultUpdateCallbackId {
        BattleResultUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: BattleResultUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<BattleResult>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<BattleResult>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `quest_id` unique index on the table `battle_result`,
/// which allows point queries on the field of the same name
/// via the [`BattleResultQuestIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.battle_result().quest_id().find(...)`.
pub struct BattleResultQuestIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<BattleResult, String>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> BattleResultTableHandle<'ctx> {
    /// Get a handle on the `quest_id` unique index on the table `battle_result`.
    pub fn quest_id(&self) -> BattleResultQuestIdUnique<'ctx> {
        BattleResultQuestIdUnique {
            imp: self.imp.get_unique_constraint::<String>("quest_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> BattleResultQuestIdUnique<'ctx> {
    /// Find the subscribed row whose `quest_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &String) -> Option<BattleResult> {
        self.imp.find(col_val)
    }
}
