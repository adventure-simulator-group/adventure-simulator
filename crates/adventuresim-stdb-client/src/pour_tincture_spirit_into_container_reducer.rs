// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PourTinctureSpiritIntoContainerArgs {
    pub character_id: u64,
    pub spirit_id: u64,
    pub object_id: u64,
}

impl From<PourTinctureSpiritIntoContainerArgs> for super::Reducer {
    fn from(args: PourTinctureSpiritIntoContainerArgs) -> Self {
        Self::PourTinctureSpiritIntoContainer {
            character_id: args.character_id,
            spirit_id: args.spirit_id,
            object_id: args.object_id,
        }
    }
}

impl __sdk::InModule for PourTinctureSpiritIntoContainerArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `pour_tincture_spirit_into_container`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait pour_tincture_spirit_into_container {
    /// Request that the remote module invoke the reducer `pour_tincture_spirit_into_container` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`pour_tincture_spirit_into_container:pour_tincture_spirit_into_container_then`] to run a callback after the reducer completes.
    fn pour_tincture_spirit_into_container(
        &self,
        character_id: u64,
        spirit_id: u64,
        object_id: u64,
    ) -> __sdk::Result<()> {
        self.pour_tincture_spirit_into_container_then(character_id, spirit_id, object_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `pour_tincture_spirit_into_container` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn pour_tincture_spirit_into_container_then(
        &self,
        character_id: u64,
        spirit_id: u64,
        object_id: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl pour_tincture_spirit_into_container for super::RemoteReducers {
    fn pour_tincture_spirit_into_container_then(
        &self,
        character_id: u64,
        spirit_id: u64,
        object_id: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            PourTinctureSpiritIntoContainerArgs {
                character_id,
                spirit_id,
                object_id,
            },
            callback,
        )
    }
}
