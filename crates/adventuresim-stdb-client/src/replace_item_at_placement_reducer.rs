// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::equipment_attachment_target_selection_type::EquipmentAttachmentTargetSelection;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ReplaceItemAtPlacementArgs {
    pub character_id: u64,
    pub inventory_item_id: u64,
    pub placement_index: u16,
    pub targets: Vec<EquipmentAttachmentTargetSelection>,
}

impl From<ReplaceItemAtPlacementArgs> for super::Reducer {
    fn from(args: ReplaceItemAtPlacementArgs) -> Self {
        Self::ReplaceItemAtPlacement {
            character_id: args.character_id,
            inventory_item_id: args.inventory_item_id,
            placement_index: args.placement_index,
            targets: args.targets,
        }
    }
}

impl __sdk::InModule for ReplaceItemAtPlacementArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `replace_item_at_placement`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait replace_item_at_placement {
    /// Request that the remote module invoke the reducer `replace_item_at_placement` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`replace_item_at_placement:replace_item_at_placement_then`] to run a callback after the reducer completes.
    fn replace_item_at_placement(
        &self,
        character_id: u64,
        inventory_item_id: u64,
        placement_index: u16,
        targets: Vec<EquipmentAttachmentTargetSelection>,
    ) -> __sdk::Result<()> {
        self.replace_item_at_placement_then(
            character_id,
            inventory_item_id,
            placement_index,
            targets,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `replace_item_at_placement` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn replace_item_at_placement_then(
        &self,
        character_id: u64,
        inventory_item_id: u64,
        placement_index: u16,
        targets: Vec<EquipmentAttachmentTargetSelection>,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl replace_item_at_placement for super::RemoteReducers {
    fn replace_item_at_placement_then(
        &self,
        character_id: u64,
        inventory_item_id: u64,
        placement_index: u16,
        targets: Vec<EquipmentAttachmentTargetSelection>,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ReplaceItemAtPlacementArgs {
                character_id,
                inventory_item_id,
                placement_index,
                targets,
            },
            callback,
        )
    }
}
