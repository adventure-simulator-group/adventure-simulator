// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct OpenCorpseArgs {
    pub actor_id: u64,
    pub corpse_id: String,
    pub action_id: String,
    pub expected_revision: u32,
    pub confirm_unauthorized: bool,
}

impl From<OpenCorpseArgs> for super::Reducer {
    fn from(args: OpenCorpseArgs) -> Self {
        Self::OpenCorpse {
            actor_id: args.actor_id,
            corpse_id: args.corpse_id,
            action_id: args.action_id,
            expected_revision: args.expected_revision,
            confirm_unauthorized: args.confirm_unauthorized,
        }
    }
}

impl __sdk::InModule for OpenCorpseArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `open_corpse`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait open_corpse {
    /// Request that the remote module invoke the reducer `open_corpse` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`open_corpse:open_corpse_then`] to run a callback after the reducer completes.
    fn open_corpse(
        &self,
        actor_id: u64,
        corpse_id: String,
        action_id: String,
        expected_revision: u32,
        confirm_unauthorized: bool,
    ) -> __sdk::Result<()> {
        self.open_corpse_then(
            actor_id,
            corpse_id,
            action_id,
            expected_revision,
            confirm_unauthorized,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `open_corpse` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn open_corpse_then(
        &self,
        actor_id: u64,
        corpse_id: String,
        action_id: String,
        expected_revision: u32,
        confirm_unauthorized: bool,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl open_corpse for super::RemoteReducers {
    fn open_corpse_then(
        &self,
        actor_id: u64,
        corpse_id: String,
        action_id: String,
        expected_revision: u32,
        confirm_unauthorized: bool,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            OpenCorpseArgs {
                actor_id,
                corpse_id,
                action_id,
                expected_revision,
                confirm_unauthorized,
            },
            callback,
        )
    }
}
