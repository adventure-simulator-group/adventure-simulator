// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ContactContextCharacterArgs {
    pub actor_id: u64,
    pub target_id: u64,
    pub contact_ref: String,
    pub expected_revision: u32,
    pub action_id: String,
}

impl From<ContactContextCharacterArgs> for super::Reducer {
    fn from(args: ContactContextCharacterArgs) -> Self {
        Self::ContactContextCharacter {
            actor_id: args.actor_id,
            target_id: args.target_id,
            contact_ref: args.contact_ref,
            expected_revision: args.expected_revision,
            action_id: args.action_id,
        }
    }
}

impl __sdk::InModule for ContactContextCharacterArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `contact_context_character`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait contact_context_character {
    /// Request that the remote module invoke the reducer `contact_context_character` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`contact_context_character:contact_context_character_then`] to run a callback after the reducer completes.
    fn contact_context_character(
        &self,
        actor_id: u64,
        target_id: u64,
        contact_ref: String,
        expected_revision: u32,
        action_id: String,
    ) -> __sdk::Result<()> {
        self.contact_context_character_then(
            actor_id,
            target_id,
            contact_ref,
            expected_revision,
            action_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `contact_context_character` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn contact_context_character_then(
        &self,
        actor_id: u64,
        target_id: u64,
        contact_ref: String,
        expected_revision: u32,
        action_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl contact_context_character for super::RemoteReducers {
    fn contact_context_character_then(
        &self,
        actor_id: u64,
        target_id: u64,
        contact_ref: String,
        expected_revision: u32,
        action_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ContactContextCharacterArgs {
                actor_id,
                target_id,
                contact_ref,
                expected_revision,
                action_id,
            },
            callback,
        )
    }
}
