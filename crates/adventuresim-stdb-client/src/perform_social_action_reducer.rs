// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{
	self as __sdk,
	__lib,
	__sats,
	__ws,
};


#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct PerformSocialActionArgs {
    pub actor_id: u64,
    pub target_id: u64,
    pub source_id: String,
    pub action_kind: String,
}

impl From<PerformSocialActionArgs> for super::Reducer {
    fn from(args: PerformSocialActionArgs) -> Self {
        Self::PerformSocialAction {
            actor_id: args.actor_id,
            target_id: args.target_id,
            source_id: args.source_id,
            action_kind: args.action_kind,
}
}
}

impl __sdk::InModule for PerformSocialActionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `perform_social_action`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait perform_social_action {
    /// Request that the remote module invoke the reducer `perform_social_action` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`perform_social_action:perform_social_action_then`] to run a callback after the reducer completes.
    fn perform_social_action(&self, actor_id: u64,
target_id: u64,
source_id: String,
action_kind: String,
) -> __sdk::Result<()> {
        self.perform_social_action_then(actor_id, target_id, source_id, action_kind,  |_, _| {})
    }

    /// Request that the remote module invoke the reducer `perform_social_action` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn perform_social_action_then(
        &self,
        actor_id: u64,
target_id: u64,
source_id: String,
action_kind: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl perform_social_action for super::RemoteReducers {
    fn perform_social_action_then(
        &self,
        actor_id: u64,
target_id: u64,
source_id: String,
action_kind: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(PerformSocialActionArgs { actor_id, target_id, source_id, action_kind,  }, callback)
    }
}

