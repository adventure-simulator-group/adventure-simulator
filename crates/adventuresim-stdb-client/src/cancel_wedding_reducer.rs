// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CancelWeddingArgs {
    pub actor_id: u64,
    pub commitment_id: String,
}

impl From<CancelWeddingArgs> for super::Reducer {
    fn from(args: CancelWeddingArgs) -> Self {
        Self::CancelWedding {
            actor_id: args.actor_id,
            commitment_id: args.commitment_id,
        }
    }
}

impl __sdk::InModule for CancelWeddingArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `cancel_wedding`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait cancel_wedding {
    /// Request that the remote module invoke the reducer `cancel_wedding` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`cancel_wedding:cancel_wedding_then`] to run a callback after the reducer completes.
    fn cancel_wedding(&self, actor_id: u64, commitment_id: String) -> __sdk::Result<()> {
        self.cancel_wedding_then(actor_id, commitment_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `cancel_wedding` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn cancel_wedding_then(
        &self,
        actor_id: u64,
        commitment_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl cancel_wedding for super::RemoteReducers {
    fn cancel_wedding_then(
        &self,
        actor_id: u64,
        commitment_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CancelWeddingArgs {
                actor_id,
                commitment_id,
            },
            callback,
        )
    }
}
