// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::residence_tier_type::ResidenceTier;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RentResidenceArgs {
    pub character_id: u64,
    pub settlement_id: String,
    pub tier: ResidenceTier,
}

impl From<RentResidenceArgs> for super::Reducer {
    fn from(args: RentResidenceArgs) -> Self {
        Self::RentResidence {
            character_id: args.character_id,
            settlement_id: args.settlement_id,
            tier: args.tier,
        }
    }
}

impl __sdk::InModule for RentResidenceArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `rent_residence`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait rent_residence {
    /// Request that the remote module invoke the reducer `rent_residence` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`rent_residence:rent_residence_then`] to run a callback after the reducer completes.
    fn rent_residence(
        &self,
        character_id: u64,
        settlement_id: String,
        tier: ResidenceTier,
    ) -> __sdk::Result<()> {
        self.rent_residence_then(character_id, settlement_id, tier, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `rent_residence` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn rent_residence_then(
        &self,
        character_id: u64,
        settlement_id: String,
        tier: ResidenceTier,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl rent_residence for super::RemoteReducers {
    fn rent_residence_then(
        &self,
        character_id: u64,
        settlement_id: String,
        tier: ResidenceTier,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RentResidenceArgs {
                character_id,
                settlement_id,
                tier,
            },
            callback,
        )
    }
}
