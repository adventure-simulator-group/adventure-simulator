// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AdministerTinctureFromContainerArgs {
    pub actor_id: u64,
    pub patient_id: u64,
    pub object_id: u64,
    pub amount_milliunits: u32,
}

impl From<AdministerTinctureFromContainerArgs> for super::Reducer {
    fn from(args: AdministerTinctureFromContainerArgs) -> Self {
        Self::AdministerTinctureFromContainer {
            actor_id: args.actor_id,
            patient_id: args.patient_id,
            object_id: args.object_id,
            amount_milliunits: args.amount_milliunits,
        }
    }
}

impl __sdk::InModule for AdministerTinctureFromContainerArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `administer_tincture_from_container`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait administer_tincture_from_container {
    /// Request that the remote module invoke the reducer `administer_tincture_from_container` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`administer_tincture_from_container:administer_tincture_from_container_then`] to run a callback after the reducer completes.
    fn administer_tincture_from_container(
        &self,
        actor_id: u64,
        patient_id: u64,
        object_id: u64,
        amount_milliunits: u32,
    ) -> __sdk::Result<()> {
        self.administer_tincture_from_container_then(
            actor_id,
            patient_id,
            object_id,
            amount_milliunits,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `administer_tincture_from_container` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn administer_tincture_from_container_then(
        &self,
        actor_id: u64,
        patient_id: u64,
        object_id: u64,
        amount_milliunits: u32,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl administer_tincture_from_container for super::RemoteReducers {
    fn administer_tincture_from_container_then(
        &self,
        actor_id: u64,
        patient_id: u64,
        object_id: u64,
        amount_milliunits: u32,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AdministerTinctureFromContainerArgs {
                actor_id,
                patient_id,
                object_id,
                amount_milliunits,
            },
            callback,
        )
    }
}
