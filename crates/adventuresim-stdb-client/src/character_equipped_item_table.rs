// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::character_equipped_item_type::CharacterEquippedItem;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `character_equipped_item`.
///
/// Obtain a handle from the [`CharacterEquippedItemTableAccess::character_equipped_item`] method on [`super::RemoteTables`],
/// like `ctx.db.character_equipped_item()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.character_equipped_item().on_insert(...)`.
pub struct CharacterEquippedItemTableHandle<'ctx> {
    imp: __sdk::TableHandle<CharacterEquippedItem>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `character_equipped_item`.
///
/// Implemented for [`super::RemoteTables`].
pub trait CharacterEquippedItemTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`CharacterEquippedItemTableHandle`], which mediates access to the table `character_equipped_item`.
    fn character_equipped_item(&self) -> CharacterEquippedItemTableHandle<'_>;
}

impl CharacterEquippedItemTableAccess for super::RemoteTables {
    fn character_equipped_item(&self) -> CharacterEquippedItemTableHandle<'_> {
        CharacterEquippedItemTableHandle {
            imp: self
                .imp
                .get_table::<CharacterEquippedItem>("character_equipped_item"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct CharacterEquippedItemInsertCallbackId(__sdk::CallbackId);
pub struct CharacterEquippedItemDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for CharacterEquippedItemTableHandle<'ctx> {
    type Row = CharacterEquippedItem;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = CharacterEquippedItem> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = CharacterEquippedItemInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> CharacterEquippedItemInsertCallbackId {
        CharacterEquippedItemInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: CharacterEquippedItemInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = CharacterEquippedItemDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> CharacterEquippedItemDeleteCallbackId {
        CharacterEquippedItemDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: CharacterEquippedItemDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct CharacterEquippedItemUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for CharacterEquippedItemTableHandle<'ctx> {
    type UpdateCallbackId = CharacterEquippedItemUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> CharacterEquippedItemUpdateCallbackId {
        CharacterEquippedItemUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: CharacterEquippedItemUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `inventory_item_id` unique index on the table `character_equipped_item`,
/// which allows point queries on the field of the same name
/// via the [`CharacterEquippedItemInventoryItemIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.character_equipped_item().inventory_item_id().find(...)`.
pub struct CharacterEquippedItemInventoryItemIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<CharacterEquippedItem, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> CharacterEquippedItemTableHandle<'ctx> {
    /// Get a handle on the `inventory_item_id` unique index on the table `character_equipped_item`.
    pub fn inventory_item_id(&self) -> CharacterEquippedItemInventoryItemIdUnique<'ctx> {
        CharacterEquippedItemInventoryItemIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("inventory_item_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> CharacterEquippedItemInventoryItemIdUnique<'ctx> {
    /// Find the subscribed row whose `inventory_item_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<CharacterEquippedItem> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<CharacterEquippedItem>("character_equipped_item");
    _table.add_unique_constraint::<u64>("inventory_item_id", |row| &row.inventory_item_id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<CharacterEquippedItem>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<CharacterEquippedItem>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `CharacterEquippedItem`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait character_equipped_itemQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `CharacterEquippedItem`.
    fn character_equipped_item(&self) -> __sdk::__query_builder::Table<CharacterEquippedItem>;
}

impl character_equipped_itemQueryTableAccess for __sdk::QueryTableAccessor {
    fn character_equipped_item(&self) -> __sdk::__query_builder::Table<CharacterEquippedItem> {
        __sdk::__query_builder::Table::new("character_equipped_item")
    }
}
