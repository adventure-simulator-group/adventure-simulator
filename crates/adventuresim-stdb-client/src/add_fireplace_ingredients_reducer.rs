// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AddFireplaceIngredientsArgs {
    pub character_id: u64,
    pub fireplace_fixture_id: String,
    pub inventory_scope: String,
    pub inventory_item_ids: Vec<u64>,
    pub fractions_micros: Vec<u32>,
}

impl From<AddFireplaceIngredientsArgs> for super::Reducer {
    fn from(args: AddFireplaceIngredientsArgs) -> Self {
        Self::AddFireplaceIngredients {
            character_id: args.character_id,
            fireplace_fixture_id: args.fireplace_fixture_id,
            inventory_scope: args.inventory_scope,
            inventory_item_ids: args.inventory_item_ids,
            fractions_micros: args.fractions_micros,
        }
    }
}

impl __sdk::InModule for AddFireplaceIngredientsArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `add_fireplace_ingredients`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait add_fireplace_ingredients {
    /// Request that the remote module invoke the reducer `add_fireplace_ingredients` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`add_fireplace_ingredients:add_fireplace_ingredients_then`] to run a callback after the reducer completes.
    fn add_fireplace_ingredients(
        &self,
        character_id: u64,
        fireplace_fixture_id: String,
        inventory_scope: String,
        inventory_item_ids: Vec<u64>,
        fractions_micros: Vec<u32>,
    ) -> __sdk::Result<()> {
        self.add_fireplace_ingredients_then(
            character_id,
            fireplace_fixture_id,
            inventory_scope,
            inventory_item_ids,
            fractions_micros,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `add_fireplace_ingredients` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn add_fireplace_ingredients_then(
        &self,
        character_id: u64,
        fireplace_fixture_id: String,
        inventory_scope: String,
        inventory_item_ids: Vec<u64>,
        fractions_micros: Vec<u32>,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl add_fireplace_ingredients for super::RemoteReducers {
    fn add_fireplace_ingredients_then(
        &self,
        character_id: u64,
        fireplace_fixture_id: String,
        inventory_scope: String,
        inventory_item_ids: Vec<u64>,
        fractions_micros: Vec<u32>,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AddFireplaceIngredientsArgs {
                character_id,
                fireplace_fixture_id,
                inventory_scope,
                inventory_item_ids,
                fractions_micros,
            },
            callback,
        )
    }
}
