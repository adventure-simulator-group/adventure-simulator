// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::residence_tier_type::ResidenceTier;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct BuyResidenceArgs {
    pub character_id: u64,
    pub settlement_id: String,
    pub tier: ResidenceTier,
}

impl From<BuyResidenceArgs> for super::Reducer {
    fn from(args: BuyResidenceArgs) -> Self {
        Self::BuyResidence {
            character_id: args.character_id,
            settlement_id: args.settlement_id,
            tier: args.tier,
        }
    }
}

impl __sdk::InModule for BuyResidenceArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `buy_residence`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait buy_residence {
    /// Request that the remote module invoke the reducer `buy_residence` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`buy_residence:buy_residence_then`] to run a callback after the reducer completes.
    fn buy_residence(
        &self,
        character_id: u64,
        settlement_id: String,
        tier: ResidenceTier,
    ) -> __sdk::Result<()> {
        self.buy_residence_then(character_id, settlement_id, tier, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `buy_residence` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn buy_residence_then(
        &self,
        character_id: u64,
        settlement_id: String,
        tier: ResidenceTier,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl buy_residence for super::RemoteReducers {
    fn buy_residence_then(
        &self,
        character_id: u64,
        settlement_id: String,
        tier: ResidenceTier,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            BuyResidenceArgs {
                character_id,
                settlement_id,
                tier,
            },
            callback,
        )
    }
}
