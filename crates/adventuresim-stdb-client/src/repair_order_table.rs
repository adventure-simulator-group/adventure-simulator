// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::equipment_attachment_target_selection_type::EquipmentAttachmentTargetSelection;
use super::repair_order_type::RepairOrder;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `repair_order`.
///
/// Obtain a handle from the [`RepairOrderTableAccess::repair_order`] method on [`super::RemoteTables`],
/// like `ctx.db.repair_order()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.repair_order().on_insert(...)`.
pub struct RepairOrderTableHandle<'ctx> {
    imp: __sdk::TableHandle<RepairOrder>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `repair_order`.
///
/// Implemented for [`super::RemoteTables`].
pub trait RepairOrderTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`RepairOrderTableHandle`], which mediates access to the table `repair_order`.
    fn repair_order(&self) -> RepairOrderTableHandle<'_>;
}

impl RepairOrderTableAccess for super::RemoteTables {
    fn repair_order(&self) -> RepairOrderTableHandle<'_> {
        RepairOrderTableHandle {
            imp: self.imp.get_table::<RepairOrder>("repair_order"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct RepairOrderInsertCallbackId(__sdk::CallbackId);
pub struct RepairOrderDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for RepairOrderTableHandle<'ctx> {
    type Row = RepairOrder;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = RepairOrder> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = RepairOrderInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> RepairOrderInsertCallbackId {
        RepairOrderInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: RepairOrderInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = RepairOrderDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> RepairOrderDeleteCallbackId {
        RepairOrderDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: RepairOrderDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct RepairOrderUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for RepairOrderTableHandle<'ctx> {
    type UpdateCallbackId = RepairOrderUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> RepairOrderUpdateCallbackId {
        RepairOrderUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: RepairOrderUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

/// Access to the `id` unique index on the table `repair_order`,
/// which allows point queries on the field of the same name
/// via the [`RepairOrderIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.repair_order().id().find(...)`.
pub struct RepairOrderIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<RepairOrder, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> RepairOrderTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `repair_order`.
    pub fn id(&self) -> RepairOrderIdUnique<'ctx> {
        RepairOrderIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> RepairOrderIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<RepairOrder> {
        self.imp.find(col_val)
    }
}

/// Access to the `inventory_item_id` unique index on the table `repair_order`,
/// which allows point queries on the field of the same name
/// via the [`RepairOrderInventoryItemIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.repair_order().inventory_item_id().find(...)`.
pub struct RepairOrderInventoryItemIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<RepairOrder, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> RepairOrderTableHandle<'ctx> {
    /// Get a handle on the `inventory_item_id` unique index on the table `repair_order`.
    pub fn inventory_item_id(&self) -> RepairOrderInventoryItemIdUnique<'ctx> {
        RepairOrderInventoryItemIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("inventory_item_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> RepairOrderInventoryItemIdUnique<'ctx> {
    /// Find the subscribed row whose `inventory_item_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<RepairOrder> {
        self.imp.find(col_val)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<RepairOrder>("repair_order");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
    _table.add_unique_constraint::<u64>("inventory_item_id", |row| &row.inventory_item_id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<RepairOrder>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<RepairOrder>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

#[allow(non_camel_case_types)]
/// Extension trait for query builder access to the table `RepairOrder`.
///
/// Implemented for [`__sdk::QueryTableAccessor`].
pub trait repair_orderQueryTableAccess {
    #[allow(non_snake_case)]
    /// Get a query builder for the table `RepairOrder`.
    fn repair_order(&self) -> __sdk::__query_builder::Table<RepairOrder>;
}

impl repair_orderQueryTableAccess for __sdk::QueryTableAccessor {
    fn repair_order(&self) -> __sdk::__query_builder::Table<RepairOrder> {
        __sdk::__query_builder::Table::new("repair_order")
    }
}
