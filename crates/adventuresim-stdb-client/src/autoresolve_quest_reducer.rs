// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AutoresolveQuestArgs {
    pub character_id: u64,
    pub quest_id: String,
}

impl From<AutoresolveQuestArgs> for super::Reducer {
    fn from(args: AutoresolveQuestArgs) -> Self {
        Self::AutoresolveQuest {
            character_id: args.character_id,
            quest_id: args.quest_id,
        }
    }
}

impl __sdk::InModule for AutoresolveQuestArgs {
    type Module = super::RemoteModule;
}

pub struct AutoresolveQuestCallbackId(__sdk::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `autoresolve_quest`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait autoresolve_quest {
    /// Request that the remote module invoke the reducer `autoresolve_quest` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_autoresolve_quest`] callbacks.
    fn autoresolve_quest(&self, character_id: u64, quest_id: String) -> __sdk::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `autoresolve_quest`.
    ///
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::ReducerEventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`AutoresolveQuestCallbackId`] can be passed to [`Self::remove_on_autoresolve_quest`]
    /// to cancel the callback.
    fn on_autoresolve_quest(
        &self,
        callback: impl FnMut(&super::ReducerEventContext, &u64, &String) + Send + 'static,
    ) -> AutoresolveQuestCallbackId;
    /// Cancel a callback previously registered by [`Self::on_autoresolve_quest`],
    /// causing it not to run in the future.
    fn remove_on_autoresolve_quest(&self, callback: AutoresolveQuestCallbackId);
}

impl autoresolve_quest for super::RemoteReducers {
    fn autoresolve_quest(&self, character_id: u64, quest_id: String) -> __sdk::Result<()> {
        self.imp.call_reducer(
            "autoresolve_quest",
            AutoresolveQuestArgs {
                character_id,
                quest_id,
            },
        )
    }
    fn on_autoresolve_quest(
        &self,
        mut callback: impl FnMut(&super::ReducerEventContext, &u64, &String) + Send + 'static,
    ) -> AutoresolveQuestCallbackId {
        AutoresolveQuestCallbackId(self.imp.on_reducer(
            "autoresolve_quest",
            Box::new(move |ctx: &super::ReducerEventContext| {
                #[allow(irrefutable_let_patterns)]
                let super::ReducerEventContext {
                    event:
                        __sdk::ReducerEvent {
                            reducer:
                                super::Reducer::AutoresolveQuest {
                                    character_id,
                                    quest_id,
                                },
                            ..
                        },
                    ..
                } = ctx
                else {
                    unreachable!()
                };
                callback(ctx, character_id, quest_id)
            }),
        ))
    }
    fn remove_on_autoresolve_quest(&self, callback: AutoresolveQuestCallbackId) {
        self.imp.remove_on_reducer("autoresolve_quest", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `autoresolve_quest`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_autoresolve_quest {
    /// Set the call-reducer flags for the reducer `autoresolve_quest` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn autoresolve_quest(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_autoresolve_quest for super::SetReducerFlags {
    fn autoresolve_quest(&self, flags: __ws::CallReducerFlags) {
        self.imp.set_call_reducer_flags("autoresolve_quest", flags);
    }
}
