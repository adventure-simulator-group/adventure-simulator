// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{
	self as __sdk,
	__lib,
	__sats,
	__ws,
};
use super::settlement_smith_type::SettlementSmith;

/// Table handle for the table `settlement_smith`.
///
/// Obtain a handle from the [`SettlementSmithTableAccess::settlement_smith`] method on [`super::RemoteTables`],
/// like `ctx.db.settlement_smith()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.settlement_smith().on_insert(...)`.
pub struct SettlementSmithTableHandle<'ctx> {
    imp: __sdk::TableHandle<SettlementSmith>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `settlement_smith`.
///
/// Implemented for [`super::RemoteTables`].
pub trait SettlementSmithTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`SettlementSmithTableHandle`], which mediates access to the table `settlement_smith`.
    fn settlement_smith(&self) -> SettlementSmithTableHandle<'_>;
}

impl SettlementSmithTableAccess for super::RemoteTables {
    fn settlement_smith(&self) -> SettlementSmithTableHandle<'_> {
        SettlementSmithTableHandle {
            imp: self.imp.get_table::<SettlementSmith>("settlement_smith"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct SettlementSmithInsertCallbackId(__sdk::CallbackId);
pub struct SettlementSmithDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for SettlementSmithTableHandle<'ctx> {
    type Row = SettlementSmith;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 { self.imp.count() }
    fn iter(&self) -> impl Iterator<Item = SettlementSmith> + '_ { self.imp.iter() }

    type InsertCallbackId = SettlementSmithInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> SettlementSmithInsertCallbackId {
        SettlementSmithInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: SettlementSmithInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = SettlementSmithDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> SettlementSmithDeleteCallbackId {
        SettlementSmithDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: SettlementSmithDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

pub struct SettlementSmithUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for SettlementSmithTableHandle<'ctx> {
    type UpdateCallbackId = SettlementSmithUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> SettlementSmithUpdateCallbackId {
        SettlementSmithUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: SettlementSmithUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

        /// Access to the `settlement_id` unique index on the table `settlement_smith`,
        /// which allows point queries on the field of the same name
        /// via the [`SettlementSmithSettlementIdUnique::find`] method.
        ///
        /// Users are encouraged not to explicitly reference this type,
        /// but to directly chain method calls,
        /// like `ctx.db.settlement_smith().settlement_id().find(...)`.
        pub struct SettlementSmithSettlementIdUnique<'ctx> {
            imp: __sdk::UniqueConstraintHandle<SettlementSmith, String>,
            phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
        }

        impl<'ctx> SettlementSmithTableHandle<'ctx> {
            /// Get a handle on the `settlement_id` unique index on the table `settlement_smith`.
            pub fn settlement_id(&self) -> SettlementSmithSettlementIdUnique<'ctx> {
                SettlementSmithSettlementIdUnique {
                    imp: self.imp.get_unique_constraint::<String>("settlement_id"),
                    phantom: std::marker::PhantomData,
                }
            }
        }

        impl<'ctx> SettlementSmithSettlementIdUnique<'ctx> {
            /// Find the subscribed row whose `settlement_id` column value is equal to `col_val`,
            /// if such a row is present in the client cache.
            pub fn find(&self, col_val: &String) -> Option<SettlementSmith> {
                self.imp.find(col_val)
            }
        }
        
#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {

    let _table = client_cache.get_or_make_table::<SettlementSmith>("settlement_smith");
    _table.add_unique_constraint::<String>("settlement_id", |row| &row.settlement_id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::v2::TableUpdate,
) -> __sdk::Result<__sdk::TableUpdate<SettlementSmith>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse(
            "TableUpdate<SettlementSmith>",
            "TableUpdate",
        ).with_cause(e).into()
    })
}

        #[allow(non_camel_case_types)]
        /// Extension trait for query builder access to the table `SettlementSmith`.
        ///
        /// Implemented for [`__sdk::QueryTableAccessor`].
        pub trait settlement_smithQueryTableAccess {
            #[allow(non_snake_case)]
            /// Get a query builder for the table `SettlementSmith`.
            fn settlement_smith(&self) -> __sdk::__query_builder::Table<SettlementSmith>;
        }

        impl settlement_smithQueryTableAccess for __sdk::QueryTableAccessor {
            fn settlement_smith(&self) -> __sdk::__query_builder::Table<SettlementSmith> {
                __sdk::__query_builder::Table::new("settlement_smith")
            }
        }

