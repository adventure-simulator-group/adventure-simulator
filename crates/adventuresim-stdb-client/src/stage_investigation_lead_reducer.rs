// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct StageInvestigationLeadArgs {
    pub character_id: u64,
    pub receipt_id: String,
    pub public_case_id: String,
    pub summary: String,
    pub safe_source_label: String,
    pub confidence_bps: u16,
    pub destination_stage: String,
    pub directions: String,
    pub exact_location_id: String,
    pub latitude_e_7: i32,
    pub longitude_e_7: i32,
    pub conflict_group: String,
    pub correction_of_lead_id: String,
}

impl From<StageInvestigationLeadArgs> for super::Reducer {
    fn from(args: StageInvestigationLeadArgs) -> Self {
        Self::StageInvestigationLead {
            character_id: args.character_id,
            receipt_id: args.receipt_id,
            public_case_id: args.public_case_id,
            summary: args.summary,
            safe_source_label: args.safe_source_label,
            confidence_bps: args.confidence_bps,
            destination_stage: args.destination_stage,
            directions: args.directions,
            exact_location_id: args.exact_location_id,
            latitude_e_7: args.latitude_e_7,
            longitude_e_7: args.longitude_e_7,
            conflict_group: args.conflict_group,
            correction_of_lead_id: args.correction_of_lead_id,
        }
    }
}

impl __sdk::InModule for StageInvestigationLeadArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `stage_investigation_lead`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait stage_investigation_lead {
    /// Request that the remote module invoke the reducer `stage_investigation_lead` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`stage_investigation_lead:stage_investigation_lead_then`] to run a callback after the reducer completes.
    fn stage_investigation_lead(
        &self,
        character_id: u64,
        receipt_id: String,
        public_case_id: String,
        summary: String,
        safe_source_label: String,
        confidence_bps: u16,
        destination_stage: String,
        directions: String,
        exact_location_id: String,
        latitude_e_7: i32,
        longitude_e_7: i32,
        conflict_group: String,
        correction_of_lead_id: String,
    ) -> __sdk::Result<()> {
        self.stage_investigation_lead_then(
            character_id,
            receipt_id,
            public_case_id,
            summary,
            safe_source_label,
            confidence_bps,
            destination_stage,
            directions,
            exact_location_id,
            latitude_e_7,
            longitude_e_7,
            conflict_group,
            correction_of_lead_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `stage_investigation_lead` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn stage_investigation_lead_then(
        &self,
        character_id: u64,
        receipt_id: String,
        public_case_id: String,
        summary: String,
        safe_source_label: String,
        confidence_bps: u16,
        destination_stage: String,
        directions: String,
        exact_location_id: String,
        latitude_e_7: i32,
        longitude_e_7: i32,
        conflict_group: String,
        correction_of_lead_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl stage_investigation_lead for super::RemoteReducers {
    fn stage_investigation_lead_then(
        &self,
        character_id: u64,
        receipt_id: String,
        public_case_id: String,
        summary: String,
        safe_source_label: String,
        confidence_bps: u16,
        destination_stage: String,
        directions: String,
        exact_location_id: String,
        latitude_e_7: i32,
        longitude_e_7: i32,
        conflict_group: String,
        correction_of_lead_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            StageInvestigationLeadArgs {
                character_id,
                receipt_id,
                public_case_id,
                summary,
                safe_source_label,
                confidence_bps,
                destination_stage,
                directions,
                exact_location_id,
                latitude_e_7,
                longitude_e_7,
                conflict_group,
                correction_of_lead_id,
            },
            callback,
        )
    }
}
