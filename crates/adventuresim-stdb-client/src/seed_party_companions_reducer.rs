// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SeedPartyCompanionsArgs {
    pub leader_id: u64,
}

impl From<SeedPartyCompanionsArgs> for super::Reducer {
    fn from(args: SeedPartyCompanionsArgs) -> Self {
        Self::SeedPartyCompanions {
            leader_id: args.leader_id,
        }
    }
}

impl __sdk::InModule for SeedPartyCompanionsArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `seed_party_companions`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait seed_party_companions {
    /// Request that the remote module invoke the reducer `seed_party_companions` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`seed_party_companions:seed_party_companions_then`] to run a callback after the reducer completes.
    fn seed_party_companions(&self, leader_id: u64) -> __sdk::Result<()> {
        self.seed_party_companions_then(leader_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `seed_party_companions` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn seed_party_companions_then(
        &self,
        leader_id: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl seed_party_companions for super::RemoteReducers {
    fn seed_party_companions_then(
        &self,
        leader_id: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp
            .invoke_reducer_with_callback(SeedPartyCompanionsArgs { leader_id }, callback)
    }
}
