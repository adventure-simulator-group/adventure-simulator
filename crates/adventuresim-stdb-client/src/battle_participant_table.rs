// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::battle_participant_type::BattleParticipant;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `battle_participant`.
///
/// Obtain a handle from the [`BattleParticipantTableAccess::battle_participant`] method on [`super::RemoteTables`],
/// like `ctx.db.battle_participant()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.battle_participant().on_insert(...)`.
pub struct BattleParticipantTableHandle<'ctx> {
    imp: __sdk::TableHandle<BattleParticipant>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `battle_participant`.
///
/// Implemented for [`super::RemoteTables`].
pub trait BattleParticipantTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`BattleParticipantTableHandle`], which mediates access to the table `battle_participant`.
    fn battle_participant(&self) -> BattleParticipantTableHandle<'_>;
}

impl BattleParticipantTableAccess for super::RemoteTables {
    fn battle_participant(&self) -> BattleParticipantTableHandle<'_> {
        BattleParticipantTableHandle {
            imp: self
                .imp
                .get_table::<BattleParticipant>("battle_participant"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct BattleParticipantInsertCallbackId(__sdk::CallbackId);
pub struct BattleParticipantDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for BattleParticipantTableHandle<'ctx> {
    type Row = BattleParticipant;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = BattleParticipant> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = BattleParticipantInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> BattleParticipantInsertCallbackId {
        BattleParticipantInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: BattleParticipantInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = BattleParticipantDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> BattleParticipantDeleteCallbackId {
        BattleParticipantDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: BattleParticipantDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<BattleParticipant>("battle_participant");
    _table.add_unique_constraint::<u64>("id", |row| &row.id);
}
pub struct BattleParticipantUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for BattleParticipantTableHandle<'ctx> {
    type UpdateCallbackId = BattleParticipantUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> BattleParticipantUpdateCallbackId {
        BattleParticipantUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: BattleParticipantUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<BattleParticipant>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<BattleParticipant>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `id` unique index on the table `battle_participant`,
/// which allows point queries on the field of the same name
/// via the [`BattleParticipantIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.battle_participant().id().find(...)`.
pub struct BattleParticipantIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<BattleParticipant, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> BattleParticipantTableHandle<'ctx> {
    /// Get a handle on the `id` unique index on the table `battle_participant`.
    pub fn id(&self) -> BattleParticipantIdUnique<'ctx> {
        BattleParticipantIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> BattleParticipantIdUnique<'ctx> {
    /// Find the subscribed row whose `id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<BattleParticipant> {
        self.imp.find(col_val)
    }
}
