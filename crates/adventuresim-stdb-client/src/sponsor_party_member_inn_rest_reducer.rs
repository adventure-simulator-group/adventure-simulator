// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SponsorPartyMemberInnRestArgs {
    pub payer_id: u64,
    pub patient_id: u64,
    pub settlement_id: String,
    pub expected_cost: u64,
}

impl From<SponsorPartyMemberInnRestArgs> for super::Reducer {
    fn from(args: SponsorPartyMemberInnRestArgs) -> Self {
        Self::SponsorPartyMemberInnRest {
            payer_id: args.payer_id,
            patient_id: args.patient_id,
            settlement_id: args.settlement_id,
            expected_cost: args.expected_cost,
        }
    }
}

impl __sdk::InModule for SponsorPartyMemberInnRestArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `sponsor_party_member_inn_rest`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait sponsor_party_member_inn_rest {
    /// Request that the remote module invoke the reducer `sponsor_party_member_inn_rest` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`sponsor_party_member_inn_rest:sponsor_party_member_inn_rest_then`] to run a callback after the reducer completes.
    fn sponsor_party_member_inn_rest(
        &self,
        payer_id: u64,
        patient_id: u64,
        settlement_id: String,
        expected_cost: u64,
    ) -> __sdk::Result<()> {
        self.sponsor_party_member_inn_rest_then(
            payer_id,
            patient_id,
            settlement_id,
            expected_cost,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `sponsor_party_member_inn_rest` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn sponsor_party_member_inn_rest_then(
        &self,
        payer_id: u64,
        patient_id: u64,
        settlement_id: String,
        expected_cost: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl sponsor_party_member_inn_rest for super::RemoteReducers {
    fn sponsor_party_member_inn_rest_then(
        &self,
        payer_id: u64,
        patient_id: u64,
        settlement_id: String,
        expected_cost: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SponsorPartyMemberInnRestArgs {
                payer_id,
                patient_id,
                settlement_id,
                expected_cost,
            },
            callback,
        )
    }
}
