// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct DismissMedicalExaminationArgs {
    pub doctor_id: u64,
    pub target_id: u64,
    pub examination_id: u64,
}

impl From<DismissMedicalExaminationArgs> for super::Reducer {
    fn from(args: DismissMedicalExaminationArgs) -> Self {
        Self::DismissMedicalExamination {
            doctor_id: args.doctor_id,
            target_id: args.target_id,
            examination_id: args.examination_id,
        }
    }
}

impl __sdk::InModule for DismissMedicalExaminationArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `dismiss_medical_examination`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait dismiss_medical_examination {
    /// Request that the remote module invoke the reducer `dismiss_medical_examination` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`dismiss_medical_examination:dismiss_medical_examination_then`] to run a callback after the reducer completes.
    fn dismiss_medical_examination(
        &self,
        doctor_id: u64,
        target_id: u64,
        examination_id: u64,
    ) -> __sdk::Result<()> {
        self.dismiss_medical_examination_then(doctor_id, target_id, examination_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `dismiss_medical_examination` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn dismiss_medical_examination_then(
        &self,
        doctor_id: u64,
        target_id: u64,
        examination_id: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl dismiss_medical_examination for super::RemoteReducers {
    fn dismiss_medical_examination_then(
        &self,
        doctor_id: u64,
        target_id: u64,
        examination_id: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            DismissMedicalExaminationArgs {
                doctor_id,
                target_id,
                examination_id,
            },
            callback,
        )
    }
}
