// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct InsertNewCharacterArgs {
    pub name: String,
    pub id: u64,
    pub temporary: bool,
}

impl From<InsertNewCharacterArgs> for super::Reducer {
    fn from(args: InsertNewCharacterArgs) -> Self {
        Self::InsertNewCharacter {
            name: args.name,
            id: args.id,
            temporary: args.temporary,
        }
    }
}

impl __sdk::InModule for InsertNewCharacterArgs {
    type Module = super::RemoteModule;
}

pub struct InsertNewCharacterCallbackId(__sdk::CallbackId);

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `insert_new_character`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait insert_new_character {
    /// Request that the remote module invoke the reducer `insert_new_character` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed by listening for [`Self::on_insert_new_character`] callbacks.
    fn insert_new_character(&self, name: String, id: u64, temporary: bool) -> __sdk::Result<()>;
    /// Register a callback to run whenever we are notified of an invocation of the reducer `insert_new_character`.
    ///
    /// Callbacks should inspect the [`__sdk::ReducerEvent`] contained in the [`super::ReducerEventContext`]
    /// to determine the reducer's status.
    ///
    /// The returned [`InsertNewCharacterCallbackId`] can be passed to [`Self::remove_on_insert_new_character`]
    /// to cancel the callback.
    fn on_insert_new_character(
        &self,
        callback: impl FnMut(&super::ReducerEventContext, &String, &u64, &bool) + Send + 'static,
    ) -> InsertNewCharacterCallbackId;
    /// Cancel a callback previously registered by [`Self::on_insert_new_character`],
    /// causing it not to run in the future.
    fn remove_on_insert_new_character(&self, callback: InsertNewCharacterCallbackId);
}

impl insert_new_character for super::RemoteReducers {
    fn insert_new_character(&self, name: String, id: u64, temporary: bool) -> __sdk::Result<()> {
        self.imp.call_reducer(
            "insert_new_character",
            InsertNewCharacterArgs {
                name,
                id,
                temporary,
            },
        )
    }
    fn on_insert_new_character(
        &self,
        mut callback: impl FnMut(&super::ReducerEventContext, &String, &u64, &bool) + Send + 'static,
    ) -> InsertNewCharacterCallbackId {
        InsertNewCharacterCallbackId(self.imp.on_reducer(
            "insert_new_character",
            Box::new(move |ctx: &super::ReducerEventContext| {
                #[allow(irrefutable_let_patterns)]
                let super::ReducerEventContext {
                    event:
                        __sdk::ReducerEvent {
                            reducer:
                                super::Reducer::InsertNewCharacter {
                                    name,
                                    id,
                                    temporary,
                                },
                            ..
                        },
                    ..
                } = ctx
                else {
                    unreachable!()
                };
                callback(ctx, name, id, temporary)
            }),
        ))
    }
    fn remove_on_insert_new_character(&self, callback: InsertNewCharacterCallbackId) {
        self.imp
            .remove_on_reducer("insert_new_character", callback.0)
    }
}

#[allow(non_camel_case_types)]
#[doc(hidden)]
/// Extension trait for setting the call-flags for the reducer `insert_new_character`.
///
/// Implemented for [`super::SetReducerFlags`].
///
/// This type is currently unstable and may be removed without a major version bump.
pub trait set_flags_for_insert_new_character {
    /// Set the call-reducer flags for the reducer `insert_new_character` to `flags`.
    ///
    /// This type is currently unstable and may be removed without a major version bump.
    fn insert_new_character(&self, flags: __ws::CallReducerFlags);
}

impl set_flags_for_insert_new_character for super::SetReducerFlags {
    fn insert_new_character(&self, flags: __ws::CallReducerFlags) {
        self.imp
            .set_call_reducer_flags("insert_new_character", flags);
    }
}
