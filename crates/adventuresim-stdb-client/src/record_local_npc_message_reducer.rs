// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct RecordLocalNpcMessageArgs {
    pub actor_id: u64,
    pub subject_id: String,
    pub npc_name: String,
    pub body: String,
}

impl From<RecordLocalNpcMessageArgs> for super::Reducer {
    fn from(args: RecordLocalNpcMessageArgs) -> Self {
        Self::RecordLocalNpcMessage {
            actor_id: args.actor_id,
            subject_id: args.subject_id,
            npc_name: args.npc_name,
            body: args.body,
        }
    }
}

impl __sdk::InModule for RecordLocalNpcMessageArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `record_local_npc_message`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait record_local_npc_message {
    /// Request that the remote module invoke the reducer `record_local_npc_message` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`record_local_npc_message:record_local_npc_message_then`] to run a callback after the reducer completes.
    fn record_local_npc_message(
        &self,
        actor_id: u64,
        subject_id: String,
        npc_name: String,
        body: String,
    ) -> __sdk::Result<()> {
        self.record_local_npc_message_then(actor_id, subject_id, npc_name, body, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `record_local_npc_message` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn record_local_npc_message_then(
        &self,
        actor_id: u64,
        subject_id: String,
        npc_name: String,
        body: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl record_local_npc_message for super::RemoteReducers {
    fn record_local_npc_message_then(
        &self,
        actor_id: u64,
        subject_id: String,
        npc_name: String,
        body: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            RecordLocalNpcMessageArgs {
                actor_id,
                subject_id,
                npc_name,
                body,
            },
            callback,
        )
    }
}
