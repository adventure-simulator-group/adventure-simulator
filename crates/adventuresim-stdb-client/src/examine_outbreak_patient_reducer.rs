// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ExamineOutbreakPatientArgs {
    pub actor_id: u64,
    pub patient_ref: String,
}

impl From<ExamineOutbreakPatientArgs> for super::Reducer {
    fn from(args: ExamineOutbreakPatientArgs) -> Self {
        Self::ExamineOutbreakPatient {
            actor_id: args.actor_id,
            patient_ref: args.patient_ref,
        }
    }
}

impl __sdk::InModule for ExamineOutbreakPatientArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `examine_outbreak_patient`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait examine_outbreak_patient {
    /// Request that the remote module invoke the reducer `examine_outbreak_patient` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`examine_outbreak_patient:examine_outbreak_patient_then`] to run a callback after the reducer completes.
    fn examine_outbreak_patient(&self, actor_id: u64, patient_ref: String) -> __sdk::Result<()> {
        self.examine_outbreak_patient_then(actor_id, patient_ref, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `examine_outbreak_patient` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn examine_outbreak_patient_then(
        &self,
        actor_id: u64,
        patient_ref: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl examine_outbreak_patient for super::RemoteReducers {
    fn examine_outbreak_patient_then(
        &self,
        actor_id: u64,
        patient_ref: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ExamineOutbreakPatientArgs {
                actor_id,
                patient_ref,
            },
            callback,
        )
    }
}
