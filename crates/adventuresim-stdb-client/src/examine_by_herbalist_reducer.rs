// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct ExamineByHerbalistArgs {
    pub patient_id: u64,
    pub settlement_id: String,
}

impl From<ExamineByHerbalistArgs> for super::Reducer {
    fn from(args: ExamineByHerbalistArgs) -> Self {
        Self::ExamineByHerbalist {
            patient_id: args.patient_id,
            settlement_id: args.settlement_id,
        }
    }
}

impl __sdk::InModule for ExamineByHerbalistArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `examine_by_herbalist`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait examine_by_herbalist {
    /// Request that the remote module invoke the reducer `examine_by_herbalist` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`examine_by_herbalist:examine_by_herbalist_then`] to run a callback after the reducer completes.
    fn examine_by_herbalist(&self, patient_id: u64, settlement_id: String) -> __sdk::Result<()> {
        self.examine_by_herbalist_then(patient_id, settlement_id, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `examine_by_herbalist` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn examine_by_herbalist_then(
        &self,
        patient_id: u64,
        settlement_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl examine_by_herbalist for super::RemoteReducers {
    fn examine_by_herbalist_then(
        &self,
        patient_id: u64,
        settlement_id: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            ExamineByHerbalistArgs {
                patient_id,
                settlement_id,
            },
            callback,
        )
    }
}
