// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SetFireplaceInstrumentArgs {
    pub character_id: u64,
    pub context_key: String,
    pub inventory_scope: String,
    pub inventory_item_id: Option<u64>,
}

impl From<SetFireplaceInstrumentArgs> for super::Reducer {
    fn from(args: SetFireplaceInstrumentArgs) -> Self {
        Self::SetFireplaceInstrument {
            character_id: args.character_id,
            context_key: args.context_key,
            inventory_scope: args.inventory_scope,
            inventory_item_id: args.inventory_item_id,
        }
    }
}

impl __sdk::InModule for SetFireplaceInstrumentArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `set_fireplace_instrument`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait set_fireplace_instrument {
    /// Request that the remote module invoke the reducer `set_fireplace_instrument` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`set_fireplace_instrument:set_fireplace_instrument_then`] to run a callback after the reducer completes.
    fn set_fireplace_instrument(
        &self,
        character_id: u64,
        context_key: String,
        inventory_scope: String,
        inventory_item_id: Option<u64>,
    ) -> __sdk::Result<()> {
        self.set_fireplace_instrument_then(
            character_id,
            context_key,
            inventory_scope,
            inventory_item_id,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `set_fireplace_instrument` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn set_fireplace_instrument_then(
        &self,
        character_id: u64,
        context_key: String,
        inventory_scope: String,
        inventory_item_id: Option<u64>,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl set_fireplace_instrument for super::RemoteReducers {
    fn set_fireplace_instrument_then(
        &self,
        character_id: u64,
        context_key: String,
        inventory_scope: String,
        inventory_item_id: Option<u64>,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SetFireplaceInstrumentArgs {
                character_id,
                context_key,
                inventory_scope,
                inventory_item_id,
            },
            callback,
        )
    }
}
