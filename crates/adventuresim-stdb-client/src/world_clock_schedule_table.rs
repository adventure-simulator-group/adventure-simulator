// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::world_clock_schedule_type::WorldClockSchedule;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `world_clock_schedule`.
///
/// Obtain a handle from the [`WorldClockScheduleTableAccess::world_clock_schedule`] method on [`super::RemoteTables`],
/// like `ctx.db.world_clock_schedule()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.world_clock_schedule().on_insert(...)`.
pub struct WorldClockScheduleTableHandle<'ctx> {
    imp: __sdk::TableHandle<WorldClockSchedule>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `world_clock_schedule`.
///
/// Implemented for [`super::RemoteTables`].
pub trait WorldClockScheduleTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`WorldClockScheduleTableHandle`], which mediates access to the table `world_clock_schedule`.
    fn world_clock_schedule(&self) -> WorldClockScheduleTableHandle<'_>;
}

impl WorldClockScheduleTableAccess for super::RemoteTables {
    fn world_clock_schedule(&self) -> WorldClockScheduleTableHandle<'_> {
        WorldClockScheduleTableHandle {
            imp: self
                .imp
                .get_table::<WorldClockSchedule>("world_clock_schedule"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct WorldClockScheduleInsertCallbackId(__sdk::CallbackId);
pub struct WorldClockScheduleDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for WorldClockScheduleTableHandle<'ctx> {
    type Row = WorldClockSchedule;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = WorldClockSchedule> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = WorldClockScheduleInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> WorldClockScheduleInsertCallbackId {
        WorldClockScheduleInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: WorldClockScheduleInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = WorldClockScheduleDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> WorldClockScheduleDeleteCallbackId {
        WorldClockScheduleDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: WorldClockScheduleDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<WorldClockSchedule>("world_clock_schedule");
    _table.add_unique_constraint::<u64>("scheduled_id", |row| &row.scheduled_id);
}
pub struct WorldClockScheduleUpdateCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::TableWithPrimaryKey for WorldClockScheduleTableHandle<'ctx> {
    type UpdateCallbackId = WorldClockScheduleUpdateCallbackId;

    fn on_update(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row, &Self::Row) + Send + 'static,
    ) -> WorldClockScheduleUpdateCallbackId {
        WorldClockScheduleUpdateCallbackId(self.imp.on_update(Box::new(callback)))
    }

    fn remove_on_update(&self, callback: WorldClockScheduleUpdateCallbackId) {
        self.imp.remove_on_update(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<WorldClockSchedule>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<WorldClockSchedule>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `scheduled_id` unique index on the table `world_clock_schedule`,
/// which allows point queries on the field of the same name
/// via the [`WorldClockScheduleScheduledIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.world_clock_schedule().scheduled_id().find(...)`.
pub struct WorldClockScheduleScheduledIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<WorldClockSchedule, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> WorldClockScheduleTableHandle<'ctx> {
    /// Get a handle on the `scheduled_id` unique index on the table `world_clock_schedule`.
    pub fn scheduled_id(&self) -> WorldClockScheduleScheduledIdUnique<'ctx> {
        WorldClockScheduleScheduledIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("scheduled_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> WorldClockScheduleScheduledIdUnique<'ctx> {
    /// Find the subscribed row whose `scheduled_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<WorldClockSchedule> {
        self.imp.find(col_val)
    }
}
