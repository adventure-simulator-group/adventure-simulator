// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SeedStandaloneTacticalMissionArgs {
    pub bootstrap_token: String,
    pub character_id: u64,
    pub mission_id: String,
    pub scene_key: String,
    pub required_enemy_kills: u32,
    pub tactical_claim: String,
}

impl From<SeedStandaloneTacticalMissionArgs> for super::Reducer {
    fn from(args: SeedStandaloneTacticalMissionArgs) -> Self {
        Self::SeedStandaloneTacticalMission {
            bootstrap_token: args.bootstrap_token,
            character_id: args.character_id,
            mission_id: args.mission_id,
            scene_key: args.scene_key,
            required_enemy_kills: args.required_enemy_kills,
            tactical_claim: args.tactical_claim,
        }
    }
}

impl __sdk::InModule for SeedStandaloneTacticalMissionArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `seed_standalone_tactical_mission`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait seed_standalone_tactical_mission {
    /// Request that the remote module invoke the reducer `seed_standalone_tactical_mission` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`seed_standalone_tactical_mission:seed_standalone_tactical_mission_then`] to run a callback after the reducer completes.
    fn seed_standalone_tactical_mission(
        &self,
        bootstrap_token: String,
        character_id: u64,
        mission_id: String,
        scene_key: String,
        required_enemy_kills: u32,
        tactical_claim: String,
    ) -> __sdk::Result<()> {
        self.seed_standalone_tactical_mission_then(
            bootstrap_token,
            character_id,
            mission_id,
            scene_key,
            required_enemy_kills,
            tactical_claim,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `seed_standalone_tactical_mission` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn seed_standalone_tactical_mission_then(
        &self,
        bootstrap_token: String,
        character_id: u64,
        mission_id: String,
        scene_key: String,
        required_enemy_kills: u32,
        tactical_claim: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl seed_standalone_tactical_mission for super::RemoteReducers {
    fn seed_standalone_tactical_mission_then(
        &self,
        bootstrap_token: String,
        character_id: u64,
        mission_id: String,
        scene_key: String,
        required_enemy_kills: u32,
        tactical_claim: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SeedStandaloneTacticalMissionArgs {
                bootstrap_token,
                character_id,
                mission_id,
                scene_key,
                required_enemy_kills,
                tactical_claim,
            },
            callback,
        )
    }
}
