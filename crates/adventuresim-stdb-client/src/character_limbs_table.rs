// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use super::character_limbs_type::CharacterLimbs;
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

/// Table handle for the table `character_limbs`.
///
/// Obtain a handle from the [`CharacterLimbsTableAccess::character_limbs`] method on [`super::RemoteTables`],
/// like `ctx.db.character_limbs()`.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.character_limbs().on_insert(...)`.
pub struct CharacterLimbsTableHandle<'ctx> {
    imp: __sdk::TableHandle<CharacterLimbs>,
    ctx: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the table `character_limbs`.
///
/// Implemented for [`super::RemoteTables`].
pub trait CharacterLimbsTableAccess {
    #[allow(non_snake_case)]
    /// Obtain a [`CharacterLimbsTableHandle`], which mediates access to the table `character_limbs`.
    fn character_limbs(&self) -> CharacterLimbsTableHandle<'_>;
}

impl CharacterLimbsTableAccess for super::RemoteTables {
    fn character_limbs(&self) -> CharacterLimbsTableHandle<'_> {
        CharacterLimbsTableHandle {
            imp: self.imp.get_table::<CharacterLimbs>("character_limbs"),
            ctx: std::marker::PhantomData,
        }
    }
}

pub struct CharacterLimbsInsertCallbackId(__sdk::CallbackId);
pub struct CharacterLimbsDeleteCallbackId(__sdk::CallbackId);

impl<'ctx> __sdk::Table for CharacterLimbsTableHandle<'ctx> {
    type Row = CharacterLimbs;
    type EventContext = super::EventContext;

    fn count(&self) -> u64 {
        self.imp.count()
    }
    fn iter(&self) -> impl Iterator<Item = CharacterLimbs> + '_ {
        self.imp.iter()
    }

    type InsertCallbackId = CharacterLimbsInsertCallbackId;

    fn on_insert(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> CharacterLimbsInsertCallbackId {
        CharacterLimbsInsertCallbackId(self.imp.on_insert(Box::new(callback)))
    }

    fn remove_on_insert(&self, callback: CharacterLimbsInsertCallbackId) {
        self.imp.remove_on_insert(callback.0)
    }

    type DeleteCallbackId = CharacterLimbsDeleteCallbackId;

    fn on_delete(
        &self,
        callback: impl FnMut(&Self::EventContext, &Self::Row) + Send + 'static,
    ) -> CharacterLimbsDeleteCallbackId {
        CharacterLimbsDeleteCallbackId(self.imp.on_delete(Box::new(callback)))
    }

    fn remove_on_delete(&self, callback: CharacterLimbsDeleteCallbackId) {
        self.imp.remove_on_delete(callback.0)
    }
}

#[doc(hidden)]
pub(super) fn register_table(client_cache: &mut __sdk::ClientCache<super::RemoteModule>) {
    let _table = client_cache.get_or_make_table::<CharacterLimbs>("character_limbs");
    _table.add_unique_constraint::<u64>("character_id", |row| &row.character_id);
}

#[doc(hidden)]
pub(super) fn parse_table_update(
    raw_updates: __ws::TableUpdate<__ws::BsatnFormat>,
) -> __sdk::Result<__sdk::TableUpdate<CharacterLimbs>> {
    __sdk::TableUpdate::parse_table_update(raw_updates).map_err(|e| {
        __sdk::InternalError::failed_parse("TableUpdate<CharacterLimbs>", "TableUpdate")
            .with_cause(e)
            .into()
    })
}

/// Access to the `character_id` unique index on the table `character_limbs`,
/// which allows point queries on the field of the same name
/// via the [`CharacterLimbsCharacterIdUnique::find`] method.
///
/// Users are encouraged not to explicitly reference this type,
/// but to directly chain method calls,
/// like `ctx.db.character_limbs().character_id().find(...)`.
pub struct CharacterLimbsCharacterIdUnique<'ctx> {
    imp: __sdk::UniqueConstraintHandle<CharacterLimbs, u64>,
    phantom: std::marker::PhantomData<&'ctx super::RemoteTables>,
}

impl<'ctx> CharacterLimbsTableHandle<'ctx> {
    /// Get a handle on the `character_id` unique index on the table `character_limbs`.
    pub fn character_id(&self) -> CharacterLimbsCharacterIdUnique<'ctx> {
        CharacterLimbsCharacterIdUnique {
            imp: self.imp.get_unique_constraint::<u64>("character_id"),
            phantom: std::marker::PhantomData,
        }
    }
}

impl<'ctx> CharacterLimbsCharacterIdUnique<'ctx> {
    /// Find the subscribed row whose `character_id` column value is equal to `col_val`,
    /// if such a row is present in the client cache.
    pub fn find(&self, col_val: &u64) -> Option<CharacterLimbs> {
        self.imp.find(col_val)
    }
}
