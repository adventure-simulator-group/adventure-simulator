// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CollectFixtureWaterIntoContainerArgs {
    pub request_id: String,
    pub character_id: u64,
    pub capability_id: String,
    pub expected_capability_version: u32,
    pub container_object_id: u64,
    pub requested_ml: u64,
}

impl From<CollectFixtureWaterIntoContainerArgs> for super::Reducer {
    fn from(args: CollectFixtureWaterIntoContainerArgs) -> Self {
        Self::CollectFixtureWaterIntoContainer {
            request_id: args.request_id,
            character_id: args.character_id,
            capability_id: args.capability_id,
            expected_capability_version: args.expected_capability_version,
            container_object_id: args.container_object_id,
            requested_ml: args.requested_ml,
        }
    }
}

impl __sdk::InModule for CollectFixtureWaterIntoContainerArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `collect_fixture_water_into_container`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait collect_fixture_water_into_container {
    /// Request that the remote module invoke the reducer `collect_fixture_water_into_container` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`collect_fixture_water_into_container:collect_fixture_water_into_container_then`] to run a callback after the reducer completes.
    fn collect_fixture_water_into_container(
        &self,
        request_id: String,
        character_id: u64,
        capability_id: String,
        expected_capability_version: u32,
        container_object_id: u64,
        requested_ml: u64,
    ) -> __sdk::Result<()> {
        self.collect_fixture_water_into_container_then(
            request_id,
            character_id,
            capability_id,
            expected_capability_version,
            container_object_id,
            requested_ml,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `collect_fixture_water_into_container` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn collect_fixture_water_into_container_then(
        &self,
        request_id: String,
        character_id: u64,
        capability_id: String,
        expected_capability_version: u32,
        container_object_id: u64,
        requested_ml: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl collect_fixture_water_into_container for super::RemoteReducers {
    fn collect_fixture_water_into_container_then(
        &self,
        request_id: String,
        character_id: u64,
        capability_id: String,
        expected_capability_version: u32,
        container_object_id: u64,
        requested_ml: u64,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CollectFixtureWaterIntoContainerArgs {
                request_id,
                character_id,
                capability_id,
                expected_capability_version,
                container_object_id,
                requested_ml,
            },
            callback,
        )
    }
}
