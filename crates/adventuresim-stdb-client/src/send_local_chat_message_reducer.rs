// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SendLocalChatMessageArgs {
    pub sender_id: u64,
    pub subject_kind: String,
    pub subject_id: String,
    pub body: String,
}

impl From<SendLocalChatMessageArgs> for super::Reducer {
    fn from(args: SendLocalChatMessageArgs) -> Self {
        Self::SendLocalChatMessage {
            sender_id: args.sender_id,
            subject_kind: args.subject_kind,
            subject_id: args.subject_id,
            body: args.body,
        }
    }
}

impl __sdk::InModule for SendLocalChatMessageArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `send_local_chat_message`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait send_local_chat_message {
    /// Request that the remote module invoke the reducer `send_local_chat_message` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`send_local_chat_message:send_local_chat_message_then`] to run a callback after the reducer completes.
    fn send_local_chat_message(
        &self,
        sender_id: u64,
        subject_kind: String,
        subject_id: String,
        body: String,
    ) -> __sdk::Result<()> {
        self.send_local_chat_message_then(sender_id, subject_kind, subject_id, body, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `send_local_chat_message` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn send_local_chat_message_then(
        &self,
        sender_id: u64,
        subject_kind: String,
        subject_id: String,
        body: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()>;
}

impl send_local_chat_message for super::RemoteReducers {
    fn send_local_chat_message_then(
        &self,
        sender_id: u64,
        subject_kind: String,
        subject_id: String,
        body: String,

        callback: impl FnOnce(
            &super::ReducerEventContext,
            Result<Result<(), String>, __sdk::InternalError>,
        ) + Send
        + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SendLocalChatMessageArgs {
                sender_id,
                subject_kind,
                subject_id,
                body,
            },
            callback,
        )
    }
}
